# StorageGenie — State

## RESUME HERE

**Version:** `0.27.0` (D57 adoption 2026-09-16: tag `contract-v0.27.0` checkout; delta = `CODER.md` echo 0.27.0 + failed-check third-state rule, `ARCHITECT.md` G-L1 provisioning-pass deferral (`D190` pin), `DISPATCH.md` version-chain pin placement, `ONBOARDING.md` step-3 pin pointer, `PROPOSING.md` PR-06 filing inputs + `LAUNCHER-DESK-REPLY` marker; 0.25.1–0.26.2 = dispatch-engine/host side only, no project steps; installed global rules file `a66aa431…` = payload `RULES.sha256` — not forked; supersedes `0.25.0`).
**Branch:** `automation` (default), evidence ref `refs/heads/storagegenie-evidence`, remote `Andovol/StorageGenie`
**Host:** `andrei@87.106.66.242:2222` → `/home/andrei/StorageGenie` (ubuntu, VPS, `VPS.md` (contract `.rules-cache/`) is the host authority)
**Status:** Phase 3 CLOSED 2026-09-14 - SG-031/033/034/035/036/037/038/040/039 all rated 98 (nine slices); exit condition blueprint:531 proved by an offline E2E on the real HTTP path + 3 source-mutation proofs; metered stage total `$0.0047543`. Phase 2 CLOSED 2026-09-12 - SG-025 to SG-030 all rated >=96 (SG-030 98); exit condition blueprint:522 proved. Phase 0 + Phase 1 closed earlier; per-slice detail: `docs/ratings.md` + worklogs + ADRs; narrative: History below.

**What is live:**
- Phase 0/1 stacks (catalog, import pipeline, expiry tracker, review UI, FTS, export) + Phase 2 AI layer (provider seam, one GO vision adapter, ANALYZING_WITH_AI + BUILDING_CANDIDATES steps, gating/provenance/ledger) + **Phase 3 layer** (model picker + safe settings endpoint, multi-item split, manual entry per field, on-screen corrections, `source_attribution` / `planning_suggestion` / `guardrail_event` tables, Cosmetics + opened-date, Daily Planning Agent on a manual button, grounded Food/Medicine chat with the adapter text op) -- AI OFF by default (SG_PROVIDER_ID=fake, SG_CONSENT=false). Receipts verified from the remote notes ref; ratings/tests: `docs/ratings.md`. **Deployed on the VPS 2026-09-14 in the production shape (SG-041): single loopback service `127.0.0.1:8003`, UI built in-image and served same-origin, DB at head `20260914_sg035_foundations` + seeded — public entry LIVE since 2026-09-15 (Launcher#34; confirmed 2026-09-16, see history).**

**What is in flight:** NONE - Phase 3 CLOSED 2026-09-14; session closed at the owner's word ("We can close the session, be sure to save all relevant context."). **NEXT ACTION (D49): a LIVE USE PASS — UNBLOCKED 2026-09-16** — the deploy/migrate/seed legs were DONE (SG-041) and the public entry is now LIVE (Launcher#34 done 2026-09-15; confirmed from the workstation 2026-09-16 — see history); what remains is owner-side: consent on, import real photos, use the screens with the two users; then Phase 4 planning. **The UI exists** (Inbox / Capture / Catalog / Review / Asset detail / Settings / Planning / Chat) — bring-up commands are the Phase 0 runbook + the Phase 3 runbook in `README.md`; `docker compose up --build -d`, `docker compose exec backend python -m alembic upgrade head`, `docker compose exec backend python -m app.seed`, health at `127.0.0.1:8003/v1/health`; the public entry is the host standard (nginx + https + login at `storagegenie.dynv6.net`) — older tunnel instructions are retired. SG-039 (work `40ae5e8`) rated 98; all nine Phase 3 slices rated 98; stage metered total `$0.0047543`; suite 180 passed + 2 base-proved decoder reds. L3 (D46) SPENT. Queued: desk `Launcher#30` open (install arc); Launcher#31 filed + D50 comment posted; the two decoder env reds; the adapter error-cost residual (destination: an adapter-touching slice); field_path namespacing + per-field accept UI (future consistency slices). **Exposure (D51 → D53 → D54/D55):** route = loopback-bound app + one nginx vhost + certbot HTTPS at `storagegenie.dynv6.net` (owner-created + DNS-verified 2026-09-14) + nginx-level login (`VPS.md` S1). **SG-041 GREEN — rated 98 (work `32363ac`): the app is DEPLOYED on the host in the production shape** (one service, `127.0.0.1:8003` loopback only, built UI served same-origin, migrated to head + seeded per D54; nothing exposed yet). **EXPOSURE REQUEST FILED: `Andovol/Launcher#34`** (2026-09-14; port 8003, hostname `storagegenie.dynv6.net`, no app login → nginx `auth_basic`; local text `docs/launcher-relay/2026-09-14-storagegenie-exposure-request.md`). Awaiting Launcher's pass; the owner approves the host write when it is taken up; then the live use pass (D49).
Phase 0 + Phase 1 slice history SG-001 to SG-024 closed (ratings 82-98; receipts/ADRs in `docs/ratings.md`; hard stops D10/D11/D18/D22 all resolved) - recoverable from `git log` + History below.
Advisories (hardening slice, none exit-blocking): attempts-rollback undercount · held/rejected jobs park in AWAITING_REVIEW · hamming length-mismatch raises · extensions flat-payload edge · enter_expiry audit before-image · missing-evidence 422-vs-404 · expiry-link per-type routing · accept-discards-edits · dot-prefix test fixture · Makefile venv paths · ensure-in-GET bootstrap smell · ADR trace imprecisions. Standing packet lines: pre-existing claims cite base proof; upload premises cite detector code; new migration heads update head-relative downgrade assertions (grep `downgrade`). compose/buildx print exit 0 even on build failure — output text is the verdict source, never the code (SG-024 G2). GO adapter lines (PIP relay 2026-09-11, re-verify live in-slice): identity headers mandatory (own UA + stable session, never `pintel-*`); `json_object` for DeepSeek kin / `json_schema` elsewhere; reject empty-200 + require non-zero usage; 200s are billable-looking but prove nothing alone.
**Open threads from that era:** shm/wal `.gitignore` done (D33); the frontend `npm run build` proof retired (landed SG-031; in-image build SG-041).
- Standing dispatch procedure (2026-09-12): background dispatches ride `job_spawn` (wake on exit) -- supersedes the old Start-Process + blocking-wait chunking (`AGENTS.md` Harness row). `--status`/`--attach` are read-only (exit before the flock); `--force` + a prior receipt makes `--status` read `done` even for a failed run (stale-receipt trap - verify via the host state word + the notes ref). Packet must be pushed before dispatch. Every packet carries the role guard: "you are the Coder; never dispatch an ID".

**Decisions not derivable from code:**
- `D1` (2026-08-28, quote m0100 "keep the code for now"): untracked Phase 0 WIP kept on disk, not committed — `backend/` FastAPI/SQLAlchemy/Alembic + `frontend/` Vite/React + `backend/data/db/storagegenie.db` (migration `0201cf10`). Pin `26e9e8b` conserved; 40 obligations `keep/covered/retire`, zero CONFLICT (disposition `m0064`).
- Owner directives: `m0045` VPS folder `/home/andrei/StorageGenie`; `m0083` Coder `grok` + shared GitHub key (selectable per slice via `CODERS.md`); Phase 0 only — no LLM/OCR/barcode/Expiry plugin until authorized.
- `m0050` grok outage (quote: "Grok is down, thats why its not working.") — SG-002 diagnosis basis; slices moved to codex.
- `m0072` formatting (quote: "Always put "My recommendation" text in bold, so I can easily see it.")
- `m0106` next-session coder/effort (quote: "We will continue with Codex on High.") — SG-005+ run codex high until countermanded.
- `m0015` blueprint suggestions verdict (quote: "No to UV3 and UV6, I don't like that directions." + "Okay to all other suggestions."): UV-3 (barcode-first ordering) and UV-6 (static dosage guardrails) REJECTED — plan order and §10 stand unchanged. UV-1/UV-2/UV-4/UV-5 + BM-1…BM-8 ACCEPTED as backlog/ADR input; tech-stack question asked next.
- `m0019` tech-stack verdict (quote: "Okay to all items."): TS-1…TS-5 ACCEPTED. Cap directive: prefer dynamic/config-driven limits over silent hard caps — every limit a named setting, logged when it binds, visible reject, no magic numbers (aligns `G-A8`; softens AGENTS.md 20 MB hard-cap narrowing — 20 MB stays as configured default with visible 413, adjustable via env).
- Register: `docs/decisions/2026-09-03-accepted-optimizations.md` — all accepted UV/BM/TS items with use case + source + slice assignment; UV-3/UV-6 recorded rejected; blueprint v3 itself unchanged.
- Owner directive 2026-09-03 (quotes: "Dec1 - ok, but please make sure you follow the new process for dispatch that you mentioned." + "you can do a cross-project handoff directly to the Launcher folder."): SG-006 APPROVED under Codex High L2 with the detached-dispatch process (Start-Process + log file, never Start-Job); cross-project Launcher handoff AUTHORIZED as files into the Launcher tree. (Not tagged `m0083` — that ID is already the `m0083` Coder-grok directive above; collision avoided.)
- `m0025` SG-005 APPROVED (quote: "Yes, approved.") — foundation verify/repair, Codex High, L2, with TS-1/TS-4/TS-5 + BM-3/BM-5 folded in. Packet + dispatch authorized.
- SG-005 dispatch LANDED and rated 97 (receipt `c3ea843` auto-published, parent/trailer/tip verified).
- Prevention relay drafted: `docs/launcher-relay/2026-09-03-publisher-restore-branch.md` (publisher must restore worktree branch after publishing; `g2b` stays). UNSENT — send with the Launcher batch after SG-005 lands; nothing commits/pushes to `automation` mid-flight.
- `D2` (2026-09-03, quote "Okay, I agree with L3."): L3 stage autonomy granted for Phase 0 exit (Tasks 11–15) under the bounds + hard stops in the RESUME status line. Effort locked to codex high via standing `m0106` unless countermanded.
- `D3` (2026-09-05, owner quote "D1 - ok"): `AGENTS.md:30` receipt-header stays at the measured wrapper echo (`contract_sync=refresh version=0.17.2`) until the `0.19.0` step-1 contract refresh re-measures it — rule-set stamp (`0.19.1`) and wrapper echo are separate facts, never hand-synced.
- `D4` (2026-09-07, owner quote "Okay to D1 and D2"): `0.19.4` record-only adoption — `AGENTS.md:4` + RESUME stamp to `0.19.4`, `RULES.md` hash re-verified unchanged; no packet change, no re-proof.
- `D5` (2026-09-07, same quote): SG-010 commit + dispatch authorized as the last L3 slice (codex high); packet re-targeted to notes-ref receipt + in-packet `coder:`/`effort:` before push (scope unchanged — the committed G7 named the retired evidence-branch publisher).
- `D7` (2026-09-07, owner quote "Okay, lets close the session here and then continue in the next session."): session closes with L3 stage complete; next session continues (D6 direction — Phase 1 planning vs live manual pass — held over, still open).
- `D6` (2026-09-08, owner quote "D6 - ok"): P1 Phase 1 planning slice next, BM-4 carried into the plan; live manual pass (compose-up/UI/restart, never executed, non-gating per review §6.3) deferred to a later discrete slice. Phase 0 deterministic-only ban lift for OCR/barcode/expiry is on the table for the Phase 1 stage grant (LLM stays banned until Phase 2 per blueprint:513).
- `D8` (2026-09-08, owner quotes "D8 - ok. Lets continue with L3 for Phase 1 and Codex." + "Start Phase 1 and do not stop until its complete."): **L3 granted for the full Phase 1 stage**, Coder codex (effort high per standing `m0106`). Bounds/hard-stops per in-flight line. L4 explicitly declined on Architect recommendation (L4 buys nothing: Phase 2 LLM/provider/money/privacy stops fire at every level per `G-K1`/`G-K2`; the Phase 1 exit checkpoint is the natural pre-AI decision point).
- `D9` (2026-09-08, owner quote "D9 - approved."): 7-slice Phase 1 stage scope approved as tabled (SG-012…SG-018 per `docs/superpowers/plans/2026-09-08-phase-1-deterministic-import.md`, committed `625d71b`). L3 sequencing to exit with no further check-ins.
- `D10` (2026-09-08, owner quote "D10 - ok."): ISS-1 (2 sandbox-blocked decoder legs) carried to the manual compose pass for in-image re-proof; L3 sequencing RESUMED SG-014→SG-018 with no further check-ins.
- `D11` (2026-09-08, owner quote "D11 - yes, approved."): SG-019 TIFF-detector repair + SG-020 exit re-run authorized to close Phase 1 under existing L3 bounds.
- `D12` (2026-09-08, owner quotes "D12 ok, lets finish Phase1 first."): manual compose pass approved FIRST (closes ISS-1 + proves image/compose/UI/restart), Phase 2 planning after. L2 single slice; hard stops unchanged.
- `D13` (2026-09-08, owner quote "D13 - ok"): desk request authorized — filed as `Andovol/Launcher#18` (docker access for dispatch identity + :8000 occupant ID). Misfile as `Andovol/StorageGenie#1` closed with pointer. Parked pending desk; port-remap decision returns to owner.
- `D14` (2026-09-09, owner quote "Okay to all 3 items."): backend host-port remap 8000→8001 approved (desk Ask 2 2026-09-08 proved `:8000` is live foreign `finnhub-mcp`, not reclaimable); SG-021 packet re-targeted (DATABASE/G1/G4/constraints/acceptance) with `VITE_API_BASE` + runbook carried; `:8000` never touched.
- `D15` (2026-09-09, same quote): rootless runtime approved per desk recommendation (shared-socket grant declined); eligibility relayed NO/NO (no <1024 binds — `8000`/`5173` per `docker-compose.yml:6,30`; no cgroup/driver opts in compose scope, Dockerfiles unchecked).
- `D16` (2026-09-09, same quote): relay to `Andovol/Launcher#18` authorized + posted (`#issuecomment-5591198761`). SG-021 fresh re-run parked pending rootless provisioning.
- `D18` (2026-09-09, owner quote "D18 - approved."): fresh SG-021 re-run FIRED under D12 bounds + D14 (`8001` remap) + D15 (rootless), L2 single slice one retry; duplicate-ID refusal falls back to a fresh packet ID with identical scope, reported not worked around. (D17 park proposal withdrawn as moot on the rootless grant.)
- `M2` (2026-09-09, self-caught): sent the transitional long verb (`SG-021 codex high`) although own STATE carried the bare-ID-accepted evidence (SG-011); cost one `bad_flag` round-trip, caught pre-slice, zero harm. Counter-rule: re-read the in-flight line's transport evidence before composing a verb.
- Runner cutover OBSERVED 2026-09-09 (desk rootless work completed it): live `dispatch` takes short verb `<ID> [--status|--attach|--force>` (`global/dispatch/dispatch:8,99`), `--force` bypasses the replay guard only (never passed to run-coder, `:129` + FLAG grep), coder/effort come from the packet head (`run-coder:195-203` — SG-021's `coder:`/`effort:` lines sufficed), packet resolves `$ID.md|$ID-*.md` exactly-one (`:211-221`, so an `r1` ID would need its own packet file). `AGENTS.md` dispatch-verb line repaired to the short form in the `0.21.1` adoption (`feaf14c`). Background wake PROVEN 2026-09-11 (probe `3b96fc80` exit 0 with harness resume; SG-023/SG-024 watches rode it) — procedure keeps chunked status-verb checks until a dispatch rides the wake.
- `D19` (2026-09-09, owner quote "D19 - approved."): desk follow-up FILED on `Andovol/Launcher#18` (`#issuecomment-5591839996`): (a) buildx-activity writable for the rootless lane, (b) 8001/ShoperOS occupant confirm + free-high-port suggestion. Port pick returns to owner after desk reply.
- `D20` (2026-09-10, owner quote "D20 - ok"): backend host port `8003` (desk recommendation ex privileged `ss` 16:28–16:29 UTC; `8000` docker-proxy/2449 · `8001` shoperos-api/761216 · `8002` shoperos-mcp/3780707 all foreign; `8003`/`5173` listener-free candidates, recheck before binding; carries through compose + `VITE_API_BASE` + runbook).
- `D21` (2026-09-10, owner quote "D21 - approved"): SG-021 attempt 3 FIRES as a fresh slice under D12 bounds + D20 (`8003`) + desk `BUILDX_CONFIG` route (project-local gitignored dir, `rootless` context kept), codex high, L2 single slice one retry, short verb `SG-021 --force` per attempt-2 precedent.
- `D23` (2026-09-11, owner session-label "D1", quote "D1 - approved."): Launcher `0.22.0` adoption as tabled (tag `contract-v0.22.0` checkout, `RULES.md` 20862B + `run-coder` 36044B CR0, `AGENTS.md` + RESUME stamp). Relabelled `D23` — `D1` is taken (2026-08-28). Committed on `automation`, pushed with the SG-022 packet (`cf2f157` chain).
- `D24` (2026-09-11, owner session-label "D2", quote "D2 and D3 - ok."): adoption commit held for session close, not pushed standalone.
- `D25` SKIPPED (avoids confusion with Launcher issue #25) — never reused per `G-P4`.
- `D26` (2026-09-11, owner session-label "D3", same quote): SG-021 re-fire HELD pending owner host-tree-clean + quota-reset confirmation — SPENT 2026-09-11 (tree observed clean by SG-022 LEG4; SG-021 scope moved to SG-023/SG-024 on opencode per D29/D30; codex-quota gate moot).
- `D28` (2026-09-11, proposed, never answered): SUPERSEDED by `D29` — codex-quota gate moot after the Coder switch.
- `D29` (2026-09-11, owner quote "Going forward use OpenCode as coder."): Coder codex→opencode; `AGENTS.md` re-selected, `m0106` superseded (history kept); effort default `medium`, uncalibrated (`G-A9`); 128 KiB packet ceiling standing.
- `D30` (2026-09-11, owner quote "D30 - approved."): SG-023 manual compose pass FIRES — SG-021 scope retargeted to opencode/`medium`, L2 single slice one retry, bounds D12+D20+`BUILDX_CONFIG` carried.
- `D31` (2026-09-11, owner quote "D31 - ok."): autonomy STAYS L2 through SG-023; L3 revisited for the Phase 2 stage after SG-023 lands (new Coder uncalibrated, live surfaces, stage not closed).
- `D32` (2026-09-11, owner quote "D32 - approved"): SG-024 FIRES — hatchling fix (lock-first, Dockerfile fallback) + rebuild + ISS-1 + remap/up/UI/restart, opencode/`medium`, L2 single slice one retry.
- `D33` (2026-09-11, owner quote "D33 - ok"): `.gitignore` gains `data/db/*.db-shm` + `*.db-wal` (committed `ef60563`, pushed).
- `D34` (2026-09-11, owner proposal "should we start the planning for Phase2 in this session and perform the actual implementation in a L3 arc in the next session? I think so."): plan-now/build-next split AGREED — Phase 2 stage plan drafted this session, L3 implementation arc next session.
- `D35` (2026-09-11, owner quote "D35 - approved"): Phase 2 stage plan APPROVED + L3 GRANTED for the next-session arc (SG-025…SG-030, opencode, one retry per slice, hard stops: BLOCKED/STOP, score <95, any new provider/privacy/money fork, any sensitive-surface touch). SG-025/026 cleared to fire; SG-027 waits on F1–F3 (now answered below).
- `F1` (2026-09-11, owner quote "I want to use OpenCode GO as the LLM provider. I have used it in other projects too, it provides multiple models."): provider = OpenCode GO (`https://opencode.ai/go`, $10/mo subscription, key via Zen console; verified from vendor docs 2026-09-11). Direct API at `https://opencode.ai/zen/go/v1/{responses|chat/completions|messages}` per model (27 models; `deepseek-v4-flash-vision-exp` is the listed vision path). SG-027 adapts to it; VPS-integration facts DELIVERED by PIP relay 2026-09-11 — full digest at `docs/launcher-relay/2026-09-11-pintel-opencode-go.md` (no secrets); plan SG-027 amended vision-spike-first (D36).
- `F2` (2026-09-11, owner quote "no caps at first, we will re-evaluate later."): spend UNCAPPED for now — encoded explicitly, re-evaluation owed; per-call cost still ledgered (`G-A3`: unmetered spend is not free spend).
- `F3` (2026-09-11, owner quote "yes"): both users in + default redaction (GPS never leaves) CONFIRMED per ADR-007 posture.
- `F4` (2026-09-11, owner quote "yes"): guardrail Stage 0 CONFIRMED (no hardcoded dosage guardrails; human confirmation mandatory regardless).
- `D36` (2026-09-11, owner quote "D36 - okay"): amended SG-027 (vision-spike-first) CONFIRMED under granted L3; no scope change beyond fail-fast ordering.
- `Q1` (2026-09-11, owner quote "it will be separate."): StorageGenie gets its OWN GO subscription — no shared pool with PIP, no 06:30Z contention to schedule around.
- `Q2` (2026-09-11, owner quote "ok"): `OPENCODE_API_KEY` placed by owner into host backend `.env` (600, runtime-group readable) at arc start, never pasted in chat.
- `D37` (2026-09-11, owner quote "Yes, close the session, make sure all the information you got from the other project for working with OpenCode GO is saved for the next session to use."): session CLOSES here; GO relay digest filed at `docs/launcher-relay/2026-09-11-pintel-opencode-go.md` (mandated deliverable); next session opens the L3 arc.
- `D38` (2026-09-12, owner quote "Okay to both D39 and D38."): SG-025 attempt-2 re-fire APPROVED with G0 quarantine-adopt clause (same ID, no `--force` — no receipt existed).
- `D39` (2026-09-12, same quote): contract `0.23.0` adoption APPROVED per tag note (checkout + stamps + `G-L1`-first session list; `RULES.md` unchanged).
- Standing harness directive (2026-09-12, owner quote "You are supposed to use the job_spawn spawner for background dispatch."): background dispatches ride `job_spawn` (wake on exit); recorded in `AGENTS.md` Harness row, supersedes Start-Process + blocking-wait chunking.
- `D42` (2026-09-12, owner quote "D42 - a, but is the lot number so relevant?"): `lot` modeled explicitly as OPTIONAL (useful for recalls/telling packs apart, never required — date stays the exit criterion) + SG-027 re-spike once APPROVED. Model-picker UI queued as SG-031 (post-exit slice, settings-backed, tested-models-only). Vendor pricing read from docs 2026-09-12: $15/mo vision cap, ~$0.001/call, 0-day retention — ledger carries computed figures from here on.
- Architect miss M1 (2026-09-07, self-caught): rewrote the RESUME status line from memory and deleted durable L3/SG-008 context; repaired by restoring verbatim + appending. Counter-rule: status lines are restore-then-append, never rewritten — diff every state edit before committing.
- Architect miss M2 (2026-09-11, self-caught twice): minted session-label IDs `D1`/`D2`/`D3` already taken (`G-P4`) and read sibling project folders unasked. Repaired via `D23`/`D24`/`D26` relabels + work rule (owner relay only). Counter-rules: mint D-numbers from the register tail, never the session count; stay in this project's tree.
- Architect miss M3 (2026-09-12, caught by desk `Launcher#30`): session-start `G-L1` compared checked-out `HEAD:VERSION` (0.22.0) against the recorded stamp — two project-local operands, the exact failure `G-L1` warns "cannot see the shared version move". Shared branch was already 0.23.0; SG-025's Coder was judged by unadopted rules. Counter-rule: every session-start check fetches `origin contract` and reads `FETCH_HEAD:VERSION`, even when both local stamps match.
- Architect miss M4 (2026-09-12, self-caught at the SG-028 audit): the SG-028 packet ceiling list omitted `dedup.py` even though its G1 explicitly required reconciling the dedup early-return path — the Coder correctly touched the required file (no penalty; packet defect recorded). Counter-rule: build the ceiling FROM the requirement list, not beside it.
- Architect miss M5 (2026-09-12, self-caught at the SG-029 audit): the folded SG-028 finding (ii) reached the packet only inside the generic phrase "caps must be ABLE to refuse before a call" — the monthly-cap-per-instance item was never restated as its own testable requirement, so the Coder fixed the router cap and left the monthly accumulator inert (`ISS-10`). Counter-rule: when folding a finding into a packet, restate it as its own explicit, testable requirement line.
- Architect miss M6 (2026-09-12, self-caught at the SG-029 audit): G0 required error rows to carry "the cost/usage it actually incurred" while the scope ceiling allowed `opencode_go.py` only for the estimate helper — an internal packet conflict; the Coder honoured the ceiling and disclosed `cost=0.0` instead (`ISS-11`). Counter-rule: before shipping a packet, check every acceptance requirement against the scope ceiling that must enable it.
- `D44` (2026-09-14, owner quote "Of course P1. I want to run the Phase 3 in L3 mode, so lets plan it and after I approved you can take it forward in L3 mode."): P1 plan-first APPROVED; Phase 3 L3 execution intent RECORDED — the L3 grant itself is still owed after the stage plan is approved (per `ARCHITECT.md:64-66,75` L3 = owner approves a stage; escalate only after a stage closes cleanly).
- `D45` (2026-09-14, owner quote "D45 and D46 both approved."): Phase 3 slice plan APPROVED (8 slices SG-031 + SG-033…SG-039, A1 shape).
- `D46` (2026-09-14, same quote): L3 GRANTED for the Phase 3 arc — opencode, medium default, one root-cause retry per slice, hard stops BLOCKED/STOP, score <95, any new provider/privacy/money fork, any off-packet sensitive-surface touch (each metered slice states its ceiling in-packet).
- `D47` (2026-09-14, owner quote "D47 - approved."): SG-040 ADDED to the stage — opened-date persistence (from SG-037 finding F-SG037-3: extracted, never written), ordered BEFORE the SG-039 exit; the stage's exit bar "Cosmetics tracked with opened-date" was otherwise unmet.
- Architect miss M7 (2026-09-14, self-caught at the SG-031 audit): the packet's tested-models premise named `deepseek-v4-flash` from MEMORY of the ratings rows instead of a grep — the rows actually record the lane Coder model `deepseek-v4.1-flash`, and no metered run used the bare id (all 7 on `-vision-exp`). The Coder corrected it with evidence (F-SG031-1), zero harm. Counter-rule: a premise citing a recorded run (model id, cost, count) is re-read from the record, never restated from memory — the hypothesis frame catches it, but catching is not the same as not writing it.
- Architect miss M8 (2026-09-14, self-caught at the SG-033 audit): the packet's G1 facts listed `evidence_ids` inside the proposal dict although the just-read lines (`candidates.py:232-245`) show no such key — evidence lives on the row (`evidence_ids_json`). Same family as M7, distinct failure: PARAPHRASE DRIFT on a just-read listing. Counter-rule: when a packet enumerates a structure's keys, quote the lines verbatim instead of paraphrasing them.
- Architect miss M9 (2026-09-14, caught by the Coder at the SG-035 audit): the ceiling omitted `test_postgres_dialect.py` although the "suite green" requirement names it by construction — every table-adding slice extends its static registry (SG-025 precedent `aeda142`, rated 98). Second instance of M4's family: the ceiling was built beside the requirement list, not from it. Counter-rule restated: derive the ceiling by walking each acceptance criterion to every file it can touch, INCLUDING test registries and derived assertions.
- Architect miss M10 (2026-09-14, caught by the Coder at the SG-037 audit): the ceiling omitted `frontend/src/App.tsx` although G2 required a new reachable route — every screen in this codebase is registered there. Third instance of the M4/M9 family (ceiling built beside the requirement list, not from it). Counter-rule: for every UI requirement, walk it to route registration, nav, and the client/types it calls BEFORE writing the ceiling.
- Architect miss M11 (2026-09-14, caught by the Coder at the SG-040 audit): the packet's G3 criterion read "an accepted (user-confirmed) `opened_date` assertion" while G1 explicitly required the field gated — a gated field commits `review_state="proposed"`, never "accepted" (the sibling `test_expiry_always_proposed_never_auto_accepted` proves it). Requirement-vs-requirement contradiction inside one packet; the Coder honoured the explicit gating instruction and disclosed it (F-SG040-1). Counter-rule: when a packet sets a review-state gate on a field, every acceptance criterion naming that field states the exact `review_state` it expects.
- `D48` (2026-09-14, owner correction, quote: "The job spawn system is not owned by the Desk, you raise issue with it only to me. ### Meanwhile the job spawn was fixed, you can retry it."): the proposed desk filing was WITHDRAWN — job-spawn/OpenCode-harness issues are raised to the OWNER only, never the desk (the m0243 post-mortem is the owner-facing record); the owner fixed the spawner the same day and authorized a retry.
- `D49` (2026-09-14, owner quote "D49 - ok."): the next door opens with a **LIVE USE PASS FIRST** — bring the stack up (compose + migrate + seed), consent on, import real photos, use the screens with the two users — and **Phase 4 planning AFTER** it (the recommendation accepted verbatim: real-world use is the untested risk, and a live pass may reorder Phase 4).
- `D50` (2026-09-14, owner quote "D50 - ok."): the two packet-contract proposals (P1 mutation-proof, P2 coder-role line) RELAYED to the Launcher batch — posted as a comment on `Andovol/Launcher#31` (https://github.com/Andovol/Launcher/issues/31#issuecomment-5663347453) with field evidence from SG-030/SG-039 (P1) and SG-028→SG-039 (P2).
- Architect miss M12 (2026-09-14, caught by the Coder at the SG-039 audit): the exit packet's cosmetics requirement said "the chat/planning catalogue carries it" while the approved SG-038 scope (Food, then Medicine) gives Cosmetics NO chat route — the Coder proved planning + chat-for-food and asserted the 422 (`F-SG039-1`). Counter-rule: before a requirement names a category on a route, read the supported-category set the shipped code enforces.
- Architect miss M13 (2026-09-14, caught by the Coder at the SG-039 audit): the packet ordered the stage tally read from "the durable `provider_call` ledger" as if the sandbox DB carried that table — it does not (production DB is at an early schema; the table exists in code paths and temp DBs). The Coder read the queryable committed fixture ledger and reconciled with the committed worklogs (`F-SG039-2`). Counter-rule: an ENVIRONMENT premise (a table exists, a service runs, a file is present) is checked against the environment, never inferred from the code that would use it.
- Architect miss M14 (2026-09-14, caught by the Coder at the SG-039 audit): the packet's "$0.05 ceiling" premise was written for a single call, but the mandated whole-corpus live leg's cumulative worst-case bound is `$0.081904` — the runner's per-fixture guard is what kept the actual at `$0.003849` (`F-SG039-3`). Counter-rule: for a corpus/batch live leg, state the cumulative worst-case bound as its own number and say which guard enforces it.
- Architect miss M15 (2026-09-14, caught by the Coder at the SG-039 audit): the ceiling omitted `backend/eval/corpus/sg029/*.json`, which `eval/run.py --live` rewrites by design — fourth instance of the M4/M9/M10 family. The Coder reverted all nine writes, protecting the frozen SG-029 baseline, and disclosed (`F-SG039-4`). Counter-rule: for a command a packet mandates, walk what that command WRITES (not just what the slice's code touches) into the ceiling; anything written-but-unlisted is either named or explicitly reverted.
- `D52` (2026-09-14, owner quote "D52 - yes, adopt it now."): contract `0.24.0` adoption EXECUTED — `.rules-cache` checked out at tag `contract-v0.24.0`; `AGENTS.md` + RESUME stamps moved to `0.24.0`; session-start byte check recorded (installed global rules file `a8ba7897…` = payload `RULES.sha256` — not forked; superseded local cache `5d7193…` = 0.23.0); tag-note checklist cleared: project `AGENTS.md` measured 7,318 B (cap 11,103 B), job_spawn Harness line already present + proven, `dispatch`/`run-coder` untouched until Launcher's host install lands.
- `D51` (2026-09-14, owner quote "D51 - no, I will need direct internet access, both mobile and desktop webpage. Check vps-info.md file and then come back to me with questions."): tunnel/loopback option REJECTED — the live-pass exposure must be DIRECT INTERNET, usable from phone + desktop browsers. `reference/vps-info.md` remains ABSENT in-tree (recorded since SG-001 report + relay F1, 2026-09-05; only sighting = the Launcher onboarding read-only pass 2026-09-02); owner asked where it can be read from. Design questions (domain/TLS, login gate, host-level route) asked; no packet until answered.
- Architect miss M16 (2026-09-14, caught by the owner): asked to check `reference/vps-info.md`, searched and read SIBLING PROJECT folders when it was absent in-tree (stay-in-tree correction 2026-09-11 re-affirmed), although the absence was already documented in our own docs and one in-tree grep showed it. Counter-rule: a cited path that is missing is checked FIRST against the project's own records, then asked of the owner — never resolved by browsing other projects.
- `D53` (2026-09-14, owner quote "I worked with Launcher Desk to find a better solution. ### Launcher moved to a new version, I want to adopt it and then you tell me if you found the answers to your questions?"): contract `0.25.0` ADOPTED (checkout + stamps + byte check `a66aa431…`); the desk solution is the new contract `VPS.md` — host STANDARDS (S1 exposure: app loopback-only → Architect files an exposure request via `PROPOSING.md` naming port/hostname/login-state → owner creates the dynv6 hostname → Launcher writes the nginx vhost + certbot cert after owner approval) + measured FACTS. D51's design questions answered: direct-internet = `https://<name>.dynv6.net` through the one nginx vhost (S1/S2); login = nginx `auth_basic` (our app has no own login → gate required, stated in the request, S3); host work is Launcher's, never ours (S1 table, S4). `reference/vps-info.md` citations retired and re-sourced to `VPS.md`. Open owner bits: hostname pick, credentials, request approval.
- `D54` (2026-09-14, owner quote "D54 D55 both ok"): SG-041 APPROVED as scoped — production exposure shape (built UI served same-origin by the backend, loopback-only `127.0.0.1:8003`, Vite dev server behind a `dev` profile, deploy proof on the host), opencode/medium, L2 one retry; the approval carries the live SQLite migrate + seed (production write); AI off, $0.
- `D55` (2026-09-14, same quote): exposure-request filing GREEN-LIT once SG-041 proves green — Architect files via `PROPOSING.md` (port 8003, hostname `storagegenie.dynv6.net` — owner-created 2026-09-14, DNS verified → `87.106.66.242` — and "no app login" → nginx `auth_basic`); Launcher's host write still needs the owner's separate approval.
- Architect miss M17 (2026-09-14, caught by the Coder at the SG-041 audit): the packet said the client "hardcodes `http://localhost:8003`" — a paraphrase of the compose dev env (`docker-compose.yml:35` old) written as source fact; `client.ts:12` is actually `VITE_API_BASE || "http://localhost:8000"`. Counter-rule: when a packet states what a file contains, it quotes the line it read — never restates an environment value as source (M8's family: paraphrase drift on just-read material).
- Architect miss M18 (2026-09-14, caught by the Coder at the SG-041 audit): the packet's acceptance asserted pre-change unmatched routes return `application/problem+json`; measured, they return plain `application/json` (the app registers its handler for `fastapi.HTTPException`, not in Starlette's router-404 MRO). An INFERENCE about which handler catches a request, written as a premise right after reading `main.py`. Counter-rule: a claim about which handler catches a request is verified by making the request (or reading the registered handler class), never inferred from a neighbouring handler's presence.
- `D56` (2026-09-14, owner quote "Okay, lets close the session."): session CLOSES — SG-041 GREEN/98 (work `32363ac`; production shape deployed under D54); exposure request filed as `Andovol/Launcher#34`; adoptions D52/D53 discharged; STATE trimmed at close (Phase 3 history rows → tombstone). Next: check Launcher#34; then the live use pass (D49) once the public entry is live.

**How owner wants work done (differs from defaults):** High-level overview in chat; full technical detail on disk. Coder does all build/deploy on VPS (`G-O1`). Confirm before any ambiguous/destructive act (`G-C1`). Batch file edits into few section-sized replacements, not many line-level ones (owner efficiency directive m0113). `My recommendation` always bold in owner-facing text (quote m0072: "Always put "My recommendation" text in bold, so I can easily see it."). Report the Coder model after every slice (owner directive 2026-09-11: opencode serves multiple models). Stay inside this project's own tree — cross-project information comes via owner relay, never by reading sibling folders directly (owner correction 2026-09-11). Plain everyday language in chat, no unexplained jargon (owner correction 2026-09-12).

---

## Findings to carry forward (project-relevant, not just Launcher)

- **Wrapper template bug (repaired):** `finalize_dispatch_report.sh` shipped with `HL-/healthylaws-evidence/worklogs` instead of `SG-/storagegenie-evidence/docs/worklogs` — patched to `SG-`/`storagegenie-evidence`/`docs/worklogs` (`/opt/storagegenie-dispatch/finalize_dispatch_report.sh:4-8`). If wrapper is reinstalled from template, re-check these three strings before next dispatch.
- **Wrapper audit false negative (REPAIRED SG-004 day):** `dispatch_coder.sh` `receipt_valid()` compared the receipt diff against hardcoded `worklogs/<ID>_report.md` instead of this project's `docs/worklogs` — repaired to `docs/worklogs/${id}_report.md` (`:106`, backup `/opt/storagegenie-dispatch/dispatch_coder.sh.bak-SG004`, `bash -n` clean, mode `root:root 755` preserved). SG-004 `3294091` was already substantively valid; the repair un-breaks P5 for future slices.
- **Wrapper effort gate relaxed (owner directive, SG-004 day):** line 37 enforced medium-only (`effort_not_approved`); replaced with default-to-medium so parsed `low|medium|high|max` all pass. Owner explicitly wants non-medium efforts available (asked High). Next `high` dispatch is the functional proof.
- **Shared host (settled 2026-09-10/11):** `:8000` answers a FOREIGN Uvicorn 404, `:8001` ShoperOS-foreign, `:8002` shoperos-mcp, `:8003`+`:5173` ours (SG-024 proved `:8003` health `ok/ok/ok` + UI + restart). Desk Launcher#18 CLOSED (rootless runtime, `BUILDX_CONFIG` project-local route, `8003` pick). Rootless docker is the runtime; `default` builder/socket denials are expected, never routed around.
- **Gates (current):** `mypy 40` advisory in 9 files (delta 0 across SG-025…SG-030); backend suite **116 collected** via `venv/bin/pytest` from `backend/` at Phase 2 close — 114 passed + the 2 decoder environment reds (`pyzbar`/`tesseract` absent, base-proved: base run `2 failed, 106 passed`); `ruff` clean; `TestClient` + frontend `npm test`/`lint`/`tsc` binding (Phase-1 era, unchanged this arc).
- **Evidence store/DB are G-K3 sensitive surfaces** (`/data/storage`, `/data/db/storagegenie.db`, host/credential) — every decision touching them needs owner sign-off individually, per `AGENTS.md`.
- **Cutover WIP absorbed (retires the D1 on-disk-WIP note):** the pre-conversion untracked code is fully superseded by the Phase 0 + Phase 1 builds above; no dispositioned obligation remains open. Kept for provenance only.
- **Effort vocabulary drift (OPEN, trivial):** `AGENTS.md` promises `xhigh`; wrapper shape is `max`. Non-medium efforts proven routinely since (codex high × many, opencode low + medium). Remaining gap is naming only — fix with next AGENTS.md touch.
- **Queued for owner approval (not applied):** shared-proposal relay for the template-drift class (`receipt_valid` literal + finalize re-parameterization + `max`/`xhigh`); Launcher repo untouched this session.
- **ISS-10 (CLOSED SG-030, FAIL-then-PASS):** the monthly cap now binds ACROSS jobs from the durable `provider_call` ledger (`reader._recorded_spend` sums committed cost for the household; worst-case estimates are compared before the first call, so a refusal makes 0 invocations / 0 rows). The adapter's per-instance `_monthly_spent` remains only as a direct-call guard (SG-030 finding 2; destination: an adapter-touching slice may remove it).
- **Phase 3 stage limits (named in the exit verdict — not defects, boundaries):** Cosmetics has NO chat route (chat is Food + Medicine, the approved SG-038 scope); planning suggestions are confirm/dismiss only with no execution path and a model-chosen `kind`; guardrail tracking is append-only rows with no analytics; a gated `opened_date` sits at `review_state="proposed"` with no per-field accept UI beyond what exists; chat history is component state, not persisted; live web enrichment/search is deferred (owner: nice-to-have); the assertion `field_path` namespacing still differs between the plugin path (`plugin:expiry-tracker/expiry_date`) and candidate-derived dates (bare) — a future consistency slice. Metered: Phase 3 total `$0.0047543` (SG-037 $0.000722 + SG-038 $0.0001833 + SG-039 $0.003849).
- **ISS-11 (CLOSED SG-030, FAIL-then-PASS):** a call that returned a body now leaves a COMMITTED row with its real cost/usage **and** `error_state` (`reader._mark_calls_failed` commits before re-raising), surviving `run_job` rollback. Residual: an adapter error raised before any body (nothing to attach) still ledgers `cost=0.0` — destination: an adapter-touching slice. **Recurred at SG-039** (the `glare` fixture failed `invalid_json` on both attempts; both rows `cost=$0.000000`) — same destination, unchanged.
- **SG-041 advisories (carried):** `docker compose config` prints host `.env` secrets (incl. the provider key) to stdout — pre-existing, never quote its output in logs; destination: the exposure slice. The production shape drops the backend `/app` source bind and `--reload` (F-SG041-3) — `--profile dev` restores the Vite server but not backend live-reload; a future dev override could restore it (not needed for the live pass).
- **Phase 3 carried advisories (re-homed from history rows at the 2026-09-14 close):** the static `EXPECTED_TABLES` registry is a maintainability trap (a derived check would remove it) · the co-evidence orphan rule over-resolves if one photo ever backs independent assets (revisit then) · PAO periods are a future type change (the field is an ISO date today) · untracked `opencode.json` (harness permission config on this workstation; predates the session) — next session: gitignore it, or commit it on the owner's word.

---

## History (recent, terse)

2026-09-10: filed Launcher#25 (`provisioning-request`: opencode state-dir grant for this lane, default stays codex; owner's all-projects-all-coders principle offered for desk scope decision) — desk cadence, not waited on. Values `proposed` (desk confirms the opencode path at step 1); `DISPATCH_PATH` stays unset per original filing.

2026-09-11: Launcher#25 CLOSED (`accepted`, `closedAt 2026-09-11T11:53:55Z`, verified via `gh issue view`): `CODER_STATE_DIR` now includes `/home/andrei/.local/share/opencode`, drop-in regenerated + `daemon-reload`, Dumpster-block gap fixed on three lanes; 128 KiB single-argument limit stands for opencode packets; lane proof (actually running opencode, D154) is ours. D23/D24/D26 recorded (session-labels D1/D2/D3 relabelled — IDs taken/skipped per `G-P4`).

2026-09-12: SG-025 attempt 1 no-receipt (opencode exit 0, 429 s, zero commits, lane dirty); desk `Launcher#30` answered + labelled `accepted-narrowed` (negative-receipt primary; status-enrichment post-install; `--diagnose` rejected; §8f install arc owner-pending, four slices). Contract delta 0.22.0→0.23.0 confirmed from `FETCH_HEAD` + tag note; adoption (D39) + re-fire (D38) with owner.

2026-09-12 (later): SG-027 attempt-2 dispatch-loop failure — ISS-9. Unit `dispatch-storagegenie@SG-027` ran to budget then `failed`; no commits, no fresh receipt, trigger result line lost at the 45-min local cap (job_spawn `b84df0d6`, exit 1 timeout). SG-027 attempt-1 STOP was rated 97 (correct fail-fast); attempt-2 (D42a re-spike) produced no work. Second instance of "failure with no project-readable evidence" → posted to `Launcher#30`; reinforces accepted proposal (b) negative receipt. No re-fire pending D43 (owner).

2026-09-12 (later still): owner-requested lane probe SG-032 GREEN (dispatch `e71f8711`, work `203d97d`, receipt valid) — lane proven healthy; failed attempt-2 left tree CLEAN (no dirt, no unpushed commits), so no salvage/cleanup needed and the failure was artifact-free (root cause still unread from the lane). Probe recorded the run line + `model unknown` per policy; minor report nit (duplicated "Scope" section), no consequence.

2026-09-12 (close of SG-027): run-2 continuation (owner-resumed the killed opencode session interactively, provider metadata `opencode-go`/`deepseek-v4.1-flash` — first lane run with a real model id) executed the packet; work `0be0315` on `601a3b3`, receipt valid; rated 98. ISS-9 corrected and CLOSED: run-1 spent its 35 min executing its own dispatch verb (`reason=lock_held`) and polling its own unit — coder-side self-dispatch, lane not at fault. Carried: banked-test reload-leak fix destination, `lot`-in-prompt question (SG-028), Pillow-12 fixture-repair disclosure. Desk #30 updated with corrected cause; future packets carry a one-line "you are the Coder; never dispatch" guard.

2026-09-12 (SG-028): pipeline wiring GREEN, work `53abefe`, rated 96 (no flag; model `deepseek-v4.1-flash` from provider metadata — second lane run with a real model id). 8-step blueprint order; default-off = Phase-1; 11 offline tests FAIL-then-PASS; carried fixes done (reload leak, schemas docstring); suite 98 passed + 2 base-proved decoder env reds; zero network, $0. Deduction 2 (coder): pipeline caps cannot bind (no estimate passed; per-step provider resets the monthly accumulator) + failed calls never ledgered (`error_state` always NULL) — fix rides SG-029 G0 (first metered slice). Packet defect M4 (ceiling omitted `dedup.py` despite a G1 requirement naming it).

2026-09-12 (SG-029): first metered baseline GREEN, work `6a07c62`, rated 97 (no flag; model `deepseek-v4.1-flash` from provider metadata). G0 repaired the per-job half of the SG-028 money findings (estimate → pre-call refusal; committed error rows surviving rollback) with both proofs FAIL-then-PASS raw; 7-image synthetic corpus + integrity/discrimination tests; runner `--live` ceiling $0.05 → one successful run, 7 calls, **$0.002328**, accuracy 0.762 / unknown_rate 0.400 / correction 0.000; prompt v1 frozen. A first metered attempt aborted (the model prepended a `{"type": "json_object"}` object) → the §5.3 retry became a real correction turn (`reader.repair_prompt`); 2 unledgered calls ≈$0.0008 disclosed. Deduction 1 (coder): the `glare` fixture's ground truth is wrong (a correct read scored 0.000). M5/M6 recorded (monthly cap never restated as a requirement; error-cost requirement vs ceiling conflict); `ISS-10`/`ISS-11` open.

2026-09-12 (SG-030, PHASE 2 CLOSE): exit E2E + runbook + close-out GREEN, work `174206c`, rated 98 (no flag; model `deepseek-v4.1-flash` from provider metadata). ISS-10 + ISS-11 CLOSED with FAIL-then-PASS both raw (durable-ledger monthly binding with pre-call refusal; committed error_state cost rows surviving rollback). `test_phase2_e2e.py` (new, 6 tests) proves blueprint:522 on the real HTTP path with zero network — Food+Medicine expiry candidates (gated fields `proposed` at confidence 0.97), atomic accept via forced-failure rollback, `needs_evidence` → manual entry (never a guessed date), auditable supersede corrections, one job-linked `provider_call` row per call, default-off unchanged; sensitivity proved by 3 source mutations instead of a fabricated pre-fix failure (disclosed as a premise difference — genuinely better proof). README Phase 2 runbook + Phase 3 non-goals; `.env.example` names. Suite 114 passed + 2 base-proved decoder reds; ruff clean; mypy delta 0; scope exact; zero spend. Stage verdict: blueprint:522 proved offline with named limits (no split/manual-entry UI, corrections API-only, no auth, caps uncapped per F2); Phase 2 CLOSED; L3 (D35) spent; Phase 3 proposal to the owner next, never auto-chained.

Self-review 2026-09-12 (Phase 2 close — six slices and a phase boundary crossed): one avoidable mistake — M5, the SG-028 money finding reached SG-029 inside a generic sentence instead of its own testable requirement line, so the monthly cap stayed inert a full slice (counter-rule recorded). One process improvement — the Coder's SG-030 mutation-proof technique (break each E2E behaviour, watch the test catch it, revert) is the right sensitivity proof for proof-of-existing-flow files; filed as a proposal with the coder-role packet line the self-dispatch failure made necessary (`docs/proposals/2026-09-12-proof-scope-and-role-line.md`). Queued-work audit: SG-031 picker, desk `Launcher#30` install arc, `npm run build` proof, Launcher template-drift relay — all still earn their place; nothing retired. Session closed at the owner's word: "Yes, lets close the session now." (D44 unanswered — carried in RESUME.)

2026-09-14: filed `Andovol/Launcher#31` (job_spawn as Class-1 dispatch transport for OpenCode lanes; PR-05 block, owner-approved text; queue titles checked, no duplicate; PR-07 clear) — desk cadence, not waited on. Local text: `docs/proposals/2026-09-14-jobspawn-dispatch-transport.md` (uncommitted).

Phase 3 slice history SG-031...SG-040 (nine slices, all rated 98) -- per-slice detail in `docs/ratings.md` + `docs/worklogs/`; recoverable from `git log`. Open advisories from those rows re-homed in Findings below.

2026-09-14 (SG-041, production exposure shape — Phase 4 prelude): GREEN, work `32363ac`, rated 98 (no flag; model unknown from argv — no `--model` flag; effort medium from process args). One default service `127.0.0.1:8003:8000`; UI built in the image and served same-origin (baked-asset property 5× dynv6 / 0× localhost:8000); `/v1` never shadowed; `/docs` intact; absent-static fallback keeps the legacy JSON root; path-traversal containment check present. Deployed on the host: migrate `0201cf10c56c` → head `20260914_sg035_foundations`, seed household `01a0a029-1477-7ca0-b200-bce78a96c679` + 2 users (left in place, D54); health ok/ok/ok; `ss` loopback-only; two consecutive bring-ups healthy; 85.85 MiB. FAIL-then-PASS raw `5 failed, 1 passed` → `6 passed`; suite 186 passed + 2 base-proved reds; ruff clean; mypy delta 0; no migration; empty frontend diff; receipt valid. Three premise corrections (F-SG041-1/2/3) all packet-side → M17/M18 + advisories. Exposure request filed same day as `Andovol/Launcher#34`; next: Launcher's host pass → live use pass (D49).

Stage self-review 2026-09-14 (Phase 3 close — nine slices, no retry needed, L3 spent): the recurring avoidable mistake is a **packet premise about the environment or a batch command** stated from memory/inference instead of measured — M13 (a DB table assumed present), M14 (a per-call ceiling applied to a corpus leg), M15 (an unlisted file the mandated command writes), plus M7 (a model id restated from memory). Code premises were verified carefully (M4/M9/M10/M11 are the ceiling-vs-requirement family, now with a standing walk-the-writes instruction). Process improvement — the Coder's two protecting moves (reverting the runner's cache writes; proving chat-for-food and asserting the 422 instead of pretending a cosmetics chat exists) are exactly the behaviour the hypothesis frame exists to produce; the frame caught four premises, zero harm. Queued work all still earns its place (desk `Launcher#30` install arc; Launcher#31 transport proposal; `npm run build` proof retired by SG-031; the two packet-contract proposals in `docs/proposals/2026-09-12-proof-scope-and-role-line.md` — P1 mutation-proof and P2 role-line both now proven in the field).

Self-review 2026-09-14 (close — SG-041 + exposure arc: two adoptions, one slice, one desk relay): one avoidable mistake class — M17/M18: packet premises about a file's content and a handler's behaviour written from restatement/inference while the material was in reach; caught by the Coder, zero harm, counter-rules recorded. One process improvement — the desk route was decisive (`VPS.md` made exposure nearly mechanical; M16's in-tree-first check became `G-T14`); carry: consult the contract's host layer BEFORE designing infra. Queued-work audit: every open item still earns its place; the frontend `npm run build` thread retired (SG-031 landed it; SG-041 builds in-image).

Self-review 2026-09-08 (Phase 1 boundary): avoidable mistake — my TIFF premise cited config, never the detector (PG-IC-09), costing SG-018's STOP + SG-019/020; counter-rule is now a standing packet line. Process improvement — environment-capability premises belong in stage plans from slice 1 (learned SG-013). Queued work all still earns its place; desk items (#18, `/opt`) pending, not mine. Proposal filed: Launcher#19 (migration-head vs head-relative rollback assertions, 3 instances).

Self-review 2026-09-11 (manual-pass close + Phase 2 planned): avoidable mistake — minted taken IDs + read out-of-tree unasked (M2 above); both caught same-session, one by me, one by the owner. Process improvement — the register tail is read BEFORE minting anything, and a project boundary is crossed only on owner wheels (relay, never reads). Queued work all still earns its place (ISS-3/5/6, npm-build proof, `/opt` desk half, template-drift relay); proposal filed: G6-quote circularity (`docs/proposals/2026-09-11-g6-local-verify-circularity.md`).
Retired 2026-09-07 (SG-001…SG-004 era rows): recoverable from `git log --oneline`; live receipts stay in the status block above.

Full onboarding narrative: `docs/launcher-onboarding-feedback.md` (for Launcher Architect).

2026-09-16 (session start + 0.27.0 adoption + exposure live): `D57` (owner: "adopt the latest version") — contract `0.25.0`→`0.27.0` ADOPTED (`.rules-cache` checkout `contract-v0.27.0` = `fc2a619`; `AGENTS.md` + RESUME stamps moved; byte check: installed global file `a66aa431…` = payload `RULES.sha256` at both ends — not forked, was behind). Delta record: 0.25.1–0.26.2 = dispatch-engine/host side only (no project adoption steps per tag notes); 0.27.0 project-facing = `CODER.md` echo 0.27.0 (future packets/receipts) + failed-check third-state rule (a check that did not run reports `unknown`/refusal, never a pass — packet premise family), `ARCHITECT.md` G-L1 provisioning-pass deferral (`D190` pin), `DISPATCH.md` version-chain pin placement, `ONBOARDING.md` step-3 pin pointer, `PROPOSING.md` PR-06 filing inputs (recorded rule-set version, proof-slice ID+packet, proposed Coder/layer-B, old wrapper path on migration) + `LAUNCHER-DESK-REPLY`/`Owed from you:` reply shape. No dispatch in flight, no provisioning pass — clean-boundary adoption. Launcher#34 DONE 2026-09-15 (desk: DNS → 87.106.66.242, certbot cert to 2026-12-14, nginx login; owner logged in once) and CONFIRMED from this workstation 2026-09-16: DNS `storagegenie.dynv6.net` → `87.106.66.242`, HTTPS `/v1/health` → 401 no-login (TLS trusted), HTTP → 301 (redirect-target string not captured). Cert verified: CN=`storagegenie.dynv6.net`, Let's Encrypt, 2026-09-15→2026-12-14. D49 live use pass UNBLOCKED (owner-side: consent on, real photos, two users). Owed to the desk on #34 (one comment, needs owner green light — infrastructure): (a) side-confirmation above, (b) upload-size answer — YES, raise it: the app accepts 20 MB uploads (`backend/app/config.py:11`, visible 413) so nginx `client_max_body_size` 1 MB would reject real photo uploads at the gate — propose `25M`; keep #34 open until the live pass proves real use, then close. Launcher#30: desk reports both options installed (engine v0.26.2, 2026-09-15 — negative receipt + `--status` elapsed/budget) and asks us to confirm on our lane + close — QUEUED: confirmable only by a future dispatch (status enrichment on any run; negative receipt only on a no-commit run); not provable from here. Launcher#31 CLOSED (verdict accepted-narrowed, applied 0.24.0; our P1/P2 relay comment posted, no desk reply pending). Launcher#35/#36/#37 (ShoperOS, owner-approved, OPEN) noted as future-packet lessons, not rules: CO-79 elapsed-as-of-report-writing, derived-set hit = FILE not node, fake-cursor SQL-predicate assertions. #32/#33 are Dumpster's, untouched. `D58` (post the #34 confirmation + 25M ask) + `D59` (start the D49 live pass) proposed to the owner.

2026-09-16 (later): `D58` APPROVED + EXECUTED — confirmation + 25M ask posted as `Launcher#34#issuecomment-5695532154`; #34 stays open until the live pass proves real use. `D59` effectively UNDERWAY owner-side (owner drove the live app: Catalog/Capture/Chat screenshots). New owner direction (Phase 4 input, pre-approval): photo-first capture — (a) paste-a-photo + mobile camera capture, (b) display name optional and photo-derived, (c) quantity/unit/asset-type photo-derived, (d) design refresh ("bland and basic"). Grounded 2026-09-16: `AssetForm.tsx` demands a typed name (`required` + "Display name required"), has drag&drop + file-select only (no paste, no `capture` attribute), and POSTs `/v1/assets` directly with no AI on that path; `Asset.display_name` is `nullable=False` (model + migration `0201cf10c56c`) and `AssetCreate.display_name` required — S2 needs a real migration on the live SQLite (G-K3 surface); `ExtractionItem` carries name/expiry/opened/date_type/lot only — quantity/unit/asset-type need a prompt+schema v2 + candidate plumbing (candidate fields are JSON, no migration); chat screenshot shows `consent_disabled` — S3 needs consent on + owner-placed provider key (Q2). `D60` APPROVED (owner quote "D60 - approved."): photo-first stage S1→S2→S3 under L3, opencode/medium, one retry per slice, per-slice dispatch green light per the approved plan. SG-042 (S1) packet pushed (`1c164d0`), dispatch green light owed. `D61` (mockups vs a pointed-at look) SUPERSEDED 2026-09-16 by owner direction: the owner provides the design system step-by-step as multiple prompts, based on `tandpfun/wardrobe`; Design prompt 1 received 2026-09-16 (theme infra + `types/product.ts` + toggle; Stone light/dark, tailwind.config.ts, next-themes, Lucide). Verified against the wardrobe repo itself (MIT): it is Vite+React with plain CSS — NO Tailwind, NO next-themes, NO TypeScript, NO Lucide (Phosphor instead), light-mode only, warm-paper tokens (`--paper #f4f0e8, --ink #191919, --muted #66625d, --line #cfc8bc, --accent #6e302e`), Instrument Sans. So the prompt's stack (tailwind/next-themes/Lucide/TS types) is wardrobe-in-spirit, not wardrobe-in-fact; the Stone palette + dark mode are the owner's own extrapolation. Q1/Q2/Q3 all APPROVED as recommended 2026-09-16 (owner: "All three as recommended."): step-1 = semantic CSS tokens (Stone light/dark + wardrobe accent/manners) + in-house ThemeProvider + Lucide toggle + `types/product.ts` as a view layer over Asset/Evidence/Job; zero new styling deps. Owner to send design prompt 2; Architect to packetize step-1 as the next design-track slice on prompt-2 arrival (SG-042 dispatch green light still owed in parallel). `D62` (consent + provider key when S2 lands) still owed. SG-042 dispatch green light still owed.
