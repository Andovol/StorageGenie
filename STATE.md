# StorageGenie — State

## RESUME HERE

**Version:** `0.22.0` (D23 adoption 2026-09-11: shared `VERSION` @ `0.22.0`, contract tag `contract-v0.22.0` @ `9baadc7`, `.rules-cache/` clean, `RULES.md` 20862B CR0 + `run-coder` 36044B CR0 verified; supersedes `0.21.1`. `0.22.0` tag-route + local-slice + guard-index changes per tag note.)
**Branch:** `automation` (default), evidence ref `refs/heads/storagegenie-evidence`, remote `Andovol/StorageGenie`
**Host:** `andrei@87.106.66.242:2222` → `/home/andrei/StorageGenie` (ubuntu, VPS, `reference/vps-info.md` is authority)
**Status:** SG-007 landed and rated 98. Backend catalog path fully proved (suite 14 passed). Receipt `d43f4c1` auto-published; parent/trailer/tip verified. `a4eba14` Makefile `clean` committed (DEC-1). **L3 GRANTED 2026-09-03 (owner quote: "Okay, I agree with L3."):** Architect sequences Phase 0 exit (Tasks 11–15: SG-008 search/export/restore → frontend verify/repair Tasks 12–14 → Task 15 exit E2E) with no per-slice approvals; bounds: codex high (m0106 stands), temp-paths only, zero prod writes/restarts/secrets/infra, one retry per slice; hard stops: BLOCKED/STOP, score <95, unrecoverable dispatch refusal, any G-K1 fork or new sensitive-surface touch. SG-008 landed and rated 98 (receipt `6d96856`, suite 23 passed, parent/trailer/tip verified). R3 stranding recurred at the SG-008 gate (`g2b_divergence_refused`), cleared by owner-run host reset to `83ac434` — publisher-restore relay still pending at the desk loop. Launcher finding filed AND committed in Launcher repo HEAD (`proposals/2026-09-03-dispatch-payload-template-drift.md`, status proposed) — desk loop owns evaluation. **SG-010 LANDED 2026-09-07, rated 98 (no flag):** exit E2E + ADR-001/002 + README runbook, suite 24 passed, notes receipt on `e9f8379` verified from remote — L3 stage Tasks 11–15 complete (all slices ≥98, no hard stop fired).
- Queued findings SG-007/SG-008 (mypy `:95`, `ALEMBIC_HEAD` literal) both CLOSED by SG-011 (rated 98); remaining 43 mypy advisories are carried scope for the next backend slice, not a queue.

- `0.18.1` adoption: sign-off A GIVEN 2026-09-05 (P1-P5, D1-D5 all approved). AGENTS.md: harness Class 4 recorded, Coder `codex` (m0106, m0083 history), stale self-refs repaired. Parameter block with desk: `docs/launcher-relay/2026-09-05-runner-install-parameter-block.md`. Version stays `0.17.2` until proof passes. STOPPED before step 6 (serialised; proof slice SG-009 needs sign-off C). 0.19.0 delta approved same day (S1–S6 one unit): provisioning request FILED as Launcher issue #8 (2026-09-05, 9 keys, XDG_RUNTIME_DIR omitted per D2) — desk steps 2–5 pending; our steps 6–9 then 0.19.0 refresh+flip resume when install lands. U1 filed as Launcher issue #10 (2026-09-05, `type: finding`, step-1 refresh-actor question); no host action until the desk verdict. #8 completed same day: two-line reply posted (`ID_PATTERN`, `LOCK`, single-quoted, verified verbatim).
- `0.19.2` adoption: D1 APPROVED 2026-09-06 (owner quote: "Yes, approved.") as one unit (`0.18.0` steps with project values → corrected `0.19.0`, SG-009 IS the step-6 vehicle per answered U1 with no separate refresh slice → record `0.19.2` once at end). #8 second round-trip POSTED 2026-09-06 (`CONTRACT_DIR='/home/andrei/storagegenie-contract'` proposed/new path, REMOTE/BRANCH measured @ `0.19.2`, `values: proposed`, verified verbatim). Installed file hash-verified ISS-115 (`8ba490ac…`). SG-009 G5 re-targeted to notes-ref receipt + in-packet `coder:`/`effort:` (scope unchanged). Steps 6–9 held for desk queue (no ping per U2) + sign-off C. Premature local `0.19.1` stamp (`d23c6b3`) stands until the record-once write; effective base `0.17.2`.
- Migration COMPLETE 2026-09-07 (desk #8): step-6 proof on SG-009 `result=done exit=0` 425s/2100s, all P PASS, receipt `d0c5a44` on `refs/notes/storagegenie-coder-reports` read from remote, `--status done`, `_UID=1000` (dispatch account, not root). Contract checkout `/home/andrei/storagegenie-contract` @ `0.19.3`. Quarantined host-ahead `6d96856` (`receipt(SG-008)`) verified present on `origin/storagegenie-evidence` — nothing lost. Step 7 repoint done by desk (ISS-132): forced command now `/usr/local/bin/dispatch` + `DISPATCH_CONF`. Transport notes: `RUN_BUDGET_S=2100` binds for the first time (35min kill real); `CONTRACT_REMOTE/BRANCH` desk-owned; `DISPATCH_PATH` is a complete PATH replacement. SG-009 rated 98 (no flag).
- Sign-off D GRANTED 2026-09-07 (owner quote: "D1 - ok"). Step-9 branch half VERIFIED NO-OP same day: `git ls-files` carries zero wrapper files (no `.sh`, no dispatch helpers — only historical packet docs, which stay); nothing deleted, nothing to delete. Desk host half (`/opt/storagegenie-dispatch/` removal) is desk-owned; desk notified on #8.
- Step 8 DONE 2026-09-07 (SG-011, first self-run through repointed command): bare-ID trigger accepted (`activating`→`done`), work `384ba99` + record `70e1943`, notes-ref receipt verified from remote-fetched ref, rated 98 (no flag). Transport notes: status verb is `SG-011 --status`; notes ref needs the explicit refspec fetch (default fetch misses it); the trigger ssh returns empty — result comes via `--status` + notes, never the trigger log. Coder-side `python`/`pytest` absent on host PATH — future packets name `venv/bin/` binaries.
**What is live:**
- `f80c6f5` AGENTS.md cutover (6406B, 0.17.2) + CLAUDE.md adapter, `5b1d1d2` decisions, `7320d6b` .gitignore — all pushed.
- Host provisioned: dispatch key `~/.ssh/storagegenie-architect-dispatch` (ed25519 432B), wrapper `/opt/storagegenie-dispatch/` (13849 + 2807, outside tree, `root:root 755`), `authorized_keys` 6 lines `restrict,pty,command=...`, GitHub repo `Andovol/StorageGenie` created, cloned to `/home/andrei/StorageGenie` (`automation`).
- Packet SG-001 `docs/packets/SG-001-host-setup-and-boundary-proof.md` + work `2abf9f1` + report `8c8997f` on `origin/automation`. Receipt `637302e` on `storagegenie-evidence` (`Dispatch-ID: SG-001`, manually repaired — see § Findings).
- Host artifacts (gitignored): `.env` 600, `venv` fastapi 0.141.1, `data/db/storagegenie.db` alembic `0201cf10c56c`, `storage_data:/data/storage`.
- Verification: `ruff PASS`, `mypy 43 advisory`, `pytest vacuous exit 5`, `npm 3 files 7 pass`, `TestClient 200 {"status":"ok","db":"ok","storage":"ok"}`. `curl :8000` hits foreign 404 (shared host), `docker volume ls` denied — both UNANSWERED/advisory.
- SG-002 (grok ×2): zero artifacts — external grok outage, owner-confirmed (see Decisions). No commits.
- SG-003 (codex medium): work `fb9546d` + `c8c58e3`, rated 82 packet-attributed (G5 omitted evidence-prefill push). Receipt blocked `candidate_not_remote` — honestly STOPped per `CO-54`.
- SG-004 (codex medium): work `90c4cab` + merge `ec4254f` (CO-54-compliant prefill, content-neutral) + `c0a14ee`; receipt `3294091` auto-published, all five properties independently verified (parent/trailer/diff/remote-head/notes). Rated 96. Wrapper P5 `FAIL` on it was the stale-literal false negative, since repaired.
- Ratings filed: `docs/ratings.md` (SG-003 82 packet, SG-004 96 coder, SG-005 97 no-deduction). Packets SG-002…SG-005 committed. `automation` @ `4532f27`, evidence @ `c3ea843`.
- SG-005 (codex high): work `324857c` (truthful health + 503, `test_health.py` 3 pass, ruff clean, lockfile + digest-pinned base, busy_timeout 5000) + merge `4532f27` (CO-54 prefill, also restored SG-004 report onto automation); receipt `c3ea843` auto-published (parent/trailer/tip verified). Rated 97. Dispatch fired once at gate (`g2b_divergence_refused` — publisher had left host on evidence branch), recovered via owner-run reset to `535a2d3`, re-trigger accepted.

- SG-006 (codex high): work `6d57bc2` (strict signature/MIME validation, dynamic bomb guard with named `max_image_pixels` setting, EXIF-free derivatives byte-proved, atomic tmp cleanup, dead-code removal, 8 committed tests incl. crafted-dimension PNG + fail-then-fix log) + merge `2d70e28` (CO-54 prefill, also restored SG-005 report onto automation); receipt `3bc9e61` auto-published (parent/trailer/tip verified). Rated 98. Waited in-turn per owner directive; detached trigger worked cleanly.

**What is in flight:** SG-021 attempt-3 packet `d1e337b` (8003 + `BUILDX_CONFIG`, D20/D21) pushed; TWO `SG-021 --force` triggers both died pre-slice on the Codex quota wall (`You've hit your usage limit`, 2nd at 21:33 EEST, no receipt, ~70k tokens burned per attempt); lane last verified clean BEFORE trigger-3 — tree state after it UNVERIFIED. Next: verify host tree clean, confirm quota reset via the usage page, re-fire `SG-021 --force` under D21. Nothing running.
Phase 1 (L3 D8+D9, codex high, LLM banned until Phase 2) CLOSED 2026-09-08 — exit PROVED (SG-020 98, chained E2E, UV-5 0.133 s). Standings, all receipts verified from the remote notes ref: SG-012 98 (job engine, `f0709f7`) · SG-013 90 (env-blocked decoder legs → ISS-1, `1dc63ec2`) · SG-014 96 (dedup/candidates/queue, `5d61d43b`) · SG-015 97 (expiry plugin, `8715d4e`) · SG-016 97 (inbox/review UI, `b8eee6c`) · SG-017 97 (FTS5/BM-4, ISS-2 closed, `d25257a`) · SG-018 96 (correct STOP, TIFF gap → ISS-3) · SG-019 98 (TIFF repair, ISS-3 closed, `d70b6f7`) · SG-020 98 (exit proof, `b4dcdc8`). Detail: `docs/ratings.md` + per-slice reports + ADRs 003/005/006/008/009. Hard-stop history: SG-013 <95 hold (D10 resume) · SG-018 BLOCKED (D11 repair+rerun). Open: ISS-1 → manual pass; desk `/opt` removal (desk-owned, no ping). First known model id: gpt-5.6-luna (SG-021 report:22).
Advisories (hardening slice, none exit-blocking): attempts-rollback undercount · held/rejected jobs park in AWAITING_REVIEW · hamming length-mismatch raises · extensions flat-payload edge · enter_expiry audit before-image · missing-evidence 422-vs-404 · expiry-link per-type routing · accept-discards-edits · dot-prefix test fixture · Makefile venv paths · ensure-in-GET bootstrap smell · ADR trace imprecisions. Standing packet lines: pre-existing claims cite base proof; upload premises cite detector code; new migration heads update head-relative downgrade assertions (grep `downgrade`). compose/buildx print exit 0 even on build failure — output text is the verdict source, never the code (SG-024 G2). GO adapter lines (PIP relay 2026-09-11, re-verify live in-slice): identity headers mandatory (own UA + stable session, never `pintel-*`); `json_object` for DeepSeek kin / `json_schema` elsewhere; reject empty-200 + require non-zero usage; 200s are billable-looking but prove nothing alone.
**L2 SG-021 BLOCKED 2026-09-08, rated 96 (no flag):** docker socket denied (exact quote in report, whole-slice stop, no workaround) + :8000 foreign Uvicorn 404 forensics (5173 closed); banked: compose config valid, `.env` 600 present, `frontend/Dockerfile` present; worklogs-only close-out, receipted + notes-ref note verified from remote. Fresh re-run 2026-09-09 (`--force` short verb): rootless green, buildx-activity read-only STOP + 8001 ShoperOS-foreign STOP, BLOCKED rated 97 (no flag), receipt fresh-verified from remote.
Parked LIFTED 2026-09-10: desk ANSWERED + CLOSED #18 (builder route = project-local `BUILDX_CONFIG` e.g. `.cache/buildx`, no host grant; ports 8000 docker-proxy/2449 · 8001 shoperos-api/761216 · 8002 shoperos-mcp/3780707 · 8003 + 5173 listener-free candidates, recommends 8003); D20/D21 APPROVED 2026-09-10 — attempt-3 packet `d1e337b` pushed. Attempt-3 trigger 2026-09-10: unit `exit-code`/`exit=1` in <~2 min, bare `failed` (no `reason=`, no work commit, no receipt) — pre-slice dispatch-loop failure, re-trigger HELD per DISPATCH §2a; D22 (desk return) with owner. Re-trigger (owner-directed) 2026-09-10 REFUSED `dirty_unattributable` — the crashed unit left dirt the gate can't attribute, so the lane is wedged until a host-side clean. Root cause PROVED from owner-pulled journal 2026-09-10 19:39 EEST: coder `codex`, unit `exit=1`, `ERROR: You've hit your usage limit ... try again at 7:03 PM`, 69,789 tokens burned; dead run left the packet-ordered `.gitignore` edit (`+.cache/buildx/`) uncommitted = the wedge dirt. Owner on host investigating manually (G-O1 overridden by explicit owner directive). Re-fire 21:33 EEST: same quota wall (whole packet fed to prompt, wall again) — reset TZ still unknown (7:03 PM is neither EEST nor elapsed); grok fallback carries two unverifieds (packet-vs-conf coder precedence, grok binary PATH with no DISPATCH_PATH). D22 desk-diagnosis MOOT (root known); #25 CLOSED 2026-09-11 (opencode state-dir granted to this lane, drop-in regenerated + `daemon-reload`, Dumpster-block gap fixed estate-wide; lane proof D154 is ours).
**SG-022 LANDED 2026-09-11, rated 97 (no flag):** opencode lane proven end-to-end (trigger→work `82312ef`→report→notes receipt, all verified from remote); host contract reads `0.22.0` (four-way version chain green); host tree observed clean 12:41 UTC (wedge dirt gone — D26 host-clean gate ready for owner word); AGENTS.md routing path corrected by Coder (`/home/andrei/storagegenie-contract/CODER.md`, queued fix); run-2 owed per `PG-DP-04`.
**SG-023 PARTIAL 2026-09-11, rated 97 (no flag):** opencode/`medium` (model `unknown` with argv+env provenance, quoted per owner rule). G1 green (docker rootless, ports mapped, config valid); G2 STOP — Dockerfile:7 `pip install --no-build-isolation --no-deps -e .` needs `hatchling.build`, absent from `requirements.lock` (deterministic over 2 attempts; apt layer proved cached); G3/G4 loudly unanswered, D20 remap deliberately withheld. Receipt valid from remote. ISS-7 CLOSED (SG-022+SG-023 = two consecutive entry-point greens). Next: SG-024 hatchling fix + rebuild + G3/G4 (D32 with owner).
**SG-024 COMPLETE 2026-09-11, rated 98 (no flag):** opencode/`medium`, model `unknown` quoted per owner rule. Churned lock correctly refused, fallback pinned `hatchling==1.32.0`+`editables==0.6` (one Dockerfile line), step-8 green; ISS-1 CLOSED (2/2 nodes + 66/66 suite in-image); D20 remap applied (README verified 0×8000/12×8003); `:8003` health ok/ok/ok + 66/66 exec + UI bytes + restart 3×healthy; zero catalog writes; stack left STOPPED (matches SG-023 end-state). D12 manual pass COMPLETE. Receipt valid from remote. Open threads: shm/wal `.gitignore` proposal (D33); `npm run build` proof queued.
**D29 DIRECTED 2026-09-11 (owner quote: "Going forward use OpenCode as coder."): Coder switches codex→opencode** (`AGENTS.md` Coder row re-selected; `m0106` superseded, kept as history). Effort default `medium` (uncalibrated per `G-A9`); packets stay far under 128 KiB argv. Next: SG-023 = SG-021 scope re-targeted to opencode (D30 with owner).
- Standing dispatch procedure (owner directive, verified 2026-09-03: a 240 s blocking call survives): dispatch turns are sequential bounded blocking waits IN-TURN until receipt or budget — no fire-and-forget, no between-turn polling, no dormant gaps. Trigger detached (`Start-Process` + log), then blocking wait loop (sleep + fetch + receipt check) inside the turn. Gate every wait on trigger acceptance within 60 s (lesson from the SG-007-day 32 min blind wait). Local quirk (2026-09-08, SG-012…SG-021): the detached trigger usually kills the local shell (`Unknown: ChildProcess.kill`) AFTER sending the verb — always verify via `--status`; a `done` line sometimes trails a stale second token — re-check before reading the receipt.

**Decisions not derivable from code:**
- `D1` (2026-08-28, quote m0100 "keep the code for now"): untracked Phase 0 WIP kept on disk, not committed — `backend/` FastAPI/SQLAlchemy/Alembic + `frontend/` Vite/React + `backend/data/db/storagegenie.db` (migration `0201cf10`). Pin `26e9e8b` conserved; 40 obligations `keep/covered/retire`, zero CONFLICT (disposition `m0064`).
- Owner directives: `m0045` VPS folder `/home/andrei/StorageGenie`; `m0083` Coder `grok` + shared GitHub key (selectable per slice via `CODERS.md`); Phase 0 only — no LLM/OCR/barcode/Expiry plugin until authorized.
- `m0050` grok outage (quote: "Grok is down, thats why its not working.") — SG-002 diagnosis basis; slices moved to codex.
- `m0072` formatting (quote: "Always put "My recommendation" text in bold, so I can easily see it.")
- `m0106` next-session coder/effort (quote: "We will continue with Codex on High.") — SG-005+ run codex high until countermanded.
- `m0015` blueprint suggestions verdict (quote: "No to UV3 and UV6, I don't like that directions." + "Okay to all other suggestions."): UV-3 (barcode-first ordering) and UV-6 (static dosage guardrails) REJECTED — plan order and §10 stand unchanged. UV-1/UV-2/UV-4/UV-5 + BM-1…BM-8 ACCEPTED as backlog/ADR input; tech-stack question asked next.
- `m0019` tech-stack verdict (quote: "Okay to all items."): TS-1…TS-5 ACCEPTED. Cap directive: prefer dynamic/config-driven limits over silent hard caps — every limit a named setting, logged when it binds, visible reject, no magic numbers (aligns `G-A8`; softens AGENTS.md 20 MB hard-cap narrowing — 20 MB stays as configured default with visible 413, adjustable via env).
- Register: `docs/decisions/2026-09-03-accepted-optimizations.md` — all accepted UV/BM/TS items with use case + source + slice assignment; UV-3/UV-6 recorded rejected; blueprint v3 itself unchanged.
- Owner directive 2026-09-03 (quotes: "Dec1 - ok, but please make sure you follow the new process for dispatch that you mentioned." + "you can do a cross-project handoff directly to the Launcher folder."): SG-006 APPROVED under Codex High L2 with the detached-dispatch process (Start-Process + log file, never Start-Job); cross-project Launcher handoff AUTHORIZED as files into the Launcher tree. (Not tagged `m0083` — that ID is already the `m0083` Coder-grok directive above; collision avoided.)
- `m0025` SG-005 APPROVED (quote: "Yes, approved.") — foundation verify/repair, Codex High, L2, with TS-1/TS-4/TS-5 + BM-3/BM-5 folded in. Packet + dispatch authorized.
- SG-005 dispatch RUNNING (re-trigger accepted after host reset to `535a2d3`; first `high` passed the effort gate — wrapper echoed `contract_sync=refresh version=0.17.2`). Watch condition: commit with `Dispatch-ID: SG-005` on `storagegenie-evidence`, then pull + audit the diff.
- Prevention relay drafted: `docs/launcher-relay/2026-09-03-publisher-restore-branch.md` (publisher must restore worktree branch after publishing; `g2b` stays). UNSENT — send with the Launcher batch after SG-005 lands; nothing commits/pushes to `automation` mid-flight.
- `D2` (2026-09-03, quote "Okay, I agree with L3."): L3 stage autonomy granted for Phase 0 exit (Tasks 11–15) under the bounds + hard stops in the RESUME status line. Effort locked to codex high via standing `m0106` unless countermanded.
- `D3` (2026-09-05, owner quote "D1 - ok"): `AGENTS.md:30` receipt-header stays at the measured wrapper echo (`contract_sync=refresh version=0.17.2`) until the `0.19.0` step-1 contract refresh re-measures it — rule-set stamp (`0.19.1`) and wrapper echo are separate facts, never hand-synced.
- `D4` (2026-09-07, owner quote "Okay to D1 and D2"): `0.19.4` record-only adoption — `AGENTS.md:4` + RESUME stamp to `0.19.4`, `RULES.md` hash re-verified unchanged; no packet change, no re-proof.
- `D5` (2026-09-07, same quote): SG-010 commit + dispatch authorized as the last L3 slice (codex high); packet re-targeted to notes-ref receipt + in-packet `coder:`/`effort:` before push (scope unchanged — the committed G7 named the retired evidence-branch publisher).
- `D7` (2026-09-07, owner quote "Okay, lets close the session here and then continue in the next session."): session closes with L3 stage complete; next session continues (D6 direction — Phase 1 planning vs live manual pass — held over, still open).
- `D6` (2026-09-08, owner quote "D6 - ok"): P1 Phase 1 planning slice next, BM-4 carried into the plan; live manual pass (compose-up/UI/restart, never executed, non-gating per review §6.3) deferred to a later discrete slice. Phase 0 deterministic-only ban lift for OCR/barcode/expiry is on the table for the Phase 1 stage grant (LLM stays banned until Phase 2 per blueprint:513).
- `D8` (2026-09-08, owner quotes "D8 - ok. Lets continue with L3 for Phase 1 and Codex." + "Start Phase 1 and do not stop until its complete."): **L3 granted for the full Phase 1 stage**, Coder codex (effort high per standing `m0106`). Bounds/hard-stops per in-flight line. L4 explicitly declined on Architect recommendation (L4 buys nothing: Phase 2 LLM/provider/money/privacy stops fire at every level per `G-K1`/`G-K2`; the Phase 1 exit checkpoint is the natural pre-AI decision point).
- `D9` (2026-09-08, owner quote "D9 - approved."): 7-slice Phase 1 stage scope approved as tabled (SG-012…SG-018 per `docs/superpowers/plans/2026-09-08-phase-1-deterministic-import.md`, committed `625d71b`). L3 sequencing to exit with no further check-ins.
- `D10` (2026-09-08, owner quote "D10 - ok."): ISS-1 (2 sandbox-blocked decoder legs) carried to the manual compose pass for in-image re-proof; L3 sequencing RESUMED SG-014→SG-018 with no further check-ins.
- `D11` (2026-09-08, owner quote "D11 - yes, approved."): SG-019 TIFF-detector repair + SG-020 exit re-run authorized to close Phase 1 under existing L3 bounds.
- `D12` (2026-09-08, owner quotes "D12 ok, lets finish Phase1 first."): manual compose pass approved FIRST (closes ISS-1 + proves image/compose/UI/restart), Phase 2 planning after. L2 single slice; hard stops unchanged.
- `D13` (2026-09-08, owner quote "D13 - ok"): desk request authorized — filed as `Andovol/Launcher#18` (docker access for dispatch identity + :8000 occupant ID). Misfile as `Andovol/StorageGenie#1` closed with pointer. Parked pending desk; port-remap decision returns to owner.
- `D14` (2026-09-09, owner quote "Okay to all 3 items."): backend host-port remap 8000→8001 approved (desk Ask 2 2026-09-08 proved `:8000` is live foreign `finnhub-mcp`, not reclaimable); SG-021 packet re-targeted (DATABASE/G1/G4/constraints/acceptance) with `VITE_API_BASE` + runbook carried; `:8000` never touched.
- `D15` (2026-09-09, same quote): rootless runtime approved per desk recommendation (shared-socket grant declined); eligibility relayed NO/NO (no <1024 binds — `8000`/`5173` per `docker-compose.yml:6,30`; no cgroup/driver opts in compose scope, Dockerfiles unchecked).
- `D16` (2026-09-09, same quote): relay to `Andovol/Launcher#18` authorized + posted (`#issuecomment-5591198761`). SG-021 fresh re-run parked pending rootless provisioning.
- `D18` (2026-09-09, owner quote "D18 - approved."): fresh SG-021 re-run FIRED under D12 bounds + D14 (`8001` remap) + D15 (rootless), L2 single slice one retry; duplicate-ID refusal falls back to a fresh packet ID with identical scope, reported not worked around. (D17 park proposal withdrawn as moot on the rootless grant.)
- `M2` (2026-09-09, self-caught): sent the transitional long verb (`SG-021 codex high`) although own STATE carried the bare-ID-accepted evidence (SG-011); cost one `bad_flag` round-trip, caught pre-slice, zero harm. Counter-rule: re-read the in-flight line's transport evidence before composing a verb.
- Runner cutover OBSERVED 2026-09-09 (desk rootless work completed it): live `dispatch` takes short verb `<ID> [--status|--attach|--force>` (`global/dispatch/dispatch:8,99`), `--force` bypasses the replay guard only (never passed to run-coder, `:129` + FLAG grep), coder/effort come from the packet head (`run-coder:195-203` — SG-021's `coder:`/`effort:` lines sufficed), packet resolves `$ID.md|$ID-*.md` exactly-one (`:211-221`, so an `r1` ID would need its own packet file). `AGENTS.md` dispatch-verb line is now stale (ISS-77 transition complete) — queued update, not applied mid-flow. `job_spawn` background primitive failed (empty list, no log byte) — Class 4 stands; the watch ran as chunked foreground status-verb checks.
- `D19` (2026-09-09, owner quote "D19 - approved."): desk follow-up FILED on `Andovol/Launcher#18` (`#issuecomment-5591839996`): (a) buildx-activity writable for the rootless lane, (b) 8001/ShoperOS occupant confirm + free-high-port suggestion. Port pick returns to owner after desk reply.
- `D20` (2026-09-10, owner quote "D20 - ok"): backend host port `8003` (desk recommendation ex privileged `ss` 16:28–16:29 UTC; `8000` docker-proxy/2449 · `8001` shoperos-api/761216 · `8002` shoperos-mcp/3780707 all foreign; `8003`/`5173` listener-free candidates, recheck before binding; carries through compose + `VITE_API_BASE` + runbook).
- `D21` (2026-09-10, owner quote "D21 - approved"): SG-021 attempt 3 FIRES as a fresh slice under D12 bounds + D20 (`8003`) + desk `BUILDX_CONFIG` route (project-local gitignored dir, `rootless` context kept), codex high, L2 single slice one retry, short verb `SG-021 --force` per attempt-2 precedent.
- `D23` (2026-09-11, owner session-label "D1", quote "D1 - approved."): Launcher `0.22.0` adoption as tabled (tag `contract-v0.22.0` checkout, `RULES.md` 20862B + `run-coder` 36044B CR0, `AGENTS.md` + RESUME stamp). Relabelled `D23` — `D1` is taken (2026-08-28). Committed on `automation`, held unpushed per D24.
- `D24` (2026-09-11, owner session-label "D2", quote "D2 and D3 - ok."): adoption commit held for session close, not pushed standalone.
- `D25` SKIPPED (avoids confusion with Launcher issue #25) — never reused per `G-P4`.
- `D26` (2026-09-11, owner session-label "D3", same quote): SG-021 re-fire stays HELD pending owner host-tree-clean + quota-reset confirmation; `0.22.0` adoption changes nothing on the host.
- `D28` (2026-09-11, proposed, never answered): SUPERSEDED by `D29` — codex-quota gate moot after the Coder switch.
- `D29` (2026-09-11, owner quote "Going forward use OpenCode as coder."): Coder codex→opencode; `AGENTS.md` re-selected, `m0106` superseded (history kept); effort default `medium`, uncalibrated (`G-A9`); 128 KiB packet ceiling standing.
- `D30` (2026-09-11, owner quote "D30 - approved."): SG-023 manual compose pass FIRES — SG-021 scope retargeted to opencode/`medium`, L2 single slice one retry, bounds D12+D20+`BUILDX_CONFIG` carried.
- `D31` (2026-09-11, owner quote "D31 - ok."): autonomy STAYS L2 through SG-023; L3 revisited for the Phase 2 stage after SG-023 lands (new Coder uncalibrated, live surfaces, stage not closed).
- `D32` (2026-09-11, owner quote "D32 - approved"): SG-024 FIRES — hatchling fix (lock-first, Dockerfile fallback) + rebuild + ISS-1 + remap/up/UI/restart, opencode/`medium`, L2 single slice one retry.
- `D33` (2026-09-11, owner quote "D33 - ok"): `.gitignore` gains `data/db/*.db-shm` + `*.db-wal` (committed `ef60563`, pushed).
- `D34` (2026-09-11, owner proposal "should we start the planning for Phase2 in this session and perform the actual implementation in a L3 arc in the next session? I think so."): plan-now/build-next split AGREED — Phase 2 stage plan drafted this session, L3 implementation arc next session.
- `D35` (2026-09-11, owner quote "D35 - approved"): Phase 2 stage plan APPROVED + L3 GRANTED for the next-session arc (SG-025…SG-030, opencode, one retry per slice, hard stops: BLOCKED/STOP, score <95, any new provider/privacy/money fork, any sensitive-surface touch). SG-025/026 cleared to fire; SG-027 waits on F1–F3 (now answered below).
- `F1` (2026-09-11, owner quote "I want to use OpenCode GO as the LLM provider. I have used it in other projects too, it provides multiple models."): provider = OpenCode GO (`https://opencode.ai/go`, $10/mo subscription, key via Zen console; verified from vendor docs 2026-09-11). Direct API at `https://opencode.ai/zen/go/v1/{responses|chat/completions|messages}` per model (27 models; `deepseek-v4-flash-vision-exp` is the listed vision path). SG-027 adapts to it; VPS-integration facts owed by cross-project relay (owner carries the prompt).
- `F2` (2026-09-11, owner quote "no caps at first, we will re-evaluate later."): spend UNCAPPED for now — encoded explicitly, re-evaluation owed; per-call cost still ledgered (`G-A3`: unmetered spend is not free spend).
- `F3` (2026-09-11, owner quote "yes"): both users in + default redaction (GPS never leaves) CONFIRMED per ADR-007 posture.
- `F4` (2026-09-11, owner quote "yes"): guardrail Stage 0 CONFIRMED (no hardcoded dosage guardrails; human confirmation mandatory regardless).
- `D36` (2026-09-11, owner quote "D36 - okay"): amended SG-027 (vision-spike-first) CONFIRMED under granted L3; no scope change beyond fail-fast ordering.
- `Q1` (2026-09-11, owner quote "it will be separate."): StorageGenie gets its OWN GO subscription — no shared pool with PIP, no 06:30Z contention to schedule around.
- `Q2` (2026-09-11, owner quote "ok"): `OPENCODE_API_KEY` placed by owner into host backend `.env` (600, runtime-group readable) at arc start, never pasted in chat.
- Architect miss M1 (2026-09-07, self-caught): rewrote the RESUME status line from memory and deleted durable L3/SG-008 context; repaired by restoring verbatim + appending. Counter-rule: status lines are restore-then-append, never rewritten — diff every state edit before committing.

**How owner wants work done (differs from defaults):** High-level overview in chat; full technical detail on disk. Coder does all build/deploy on VPS (`G-O1`). Confirm before any ambiguous/destructive act (`G-C1`). Batch file edits into few section-sized replacements, not many line-level ones (owner efficiency directive m0113). `My recommendation` always bold in owner-facing text (quote m0072: "Always put "My recommendation" text in bold, so I can easily see it."). Report the Coder model after every slice (owner directive 2026-09-11: opencode serves multiple models). Stay inside this project's own tree — cross-project information comes via owner relay, never by reading sibling folders directly (owner correction 2026-09-11).

---

## Findings to carry forward (project-relevant, not just Launcher)

- **Wrapper template bug (repaired):** `finalize_dispatch_report.sh` shipped with `HL-/healthylaws-evidence/worklogs` instead of `SG-/storagegenie-evidence/docs/worklogs` — patched to `SG-`/`storagegenie-evidence`/`docs/worklogs` (`/opt/storagegenie-dispatch/finalize_dispatch_report.sh:4-8`). If wrapper is reinstalled from template, re-check these three strings before next dispatch.
- **Wrapper audit false negative (REPAIRED SG-004 day):** `dispatch_coder.sh` `receipt_valid()` compared the receipt diff against hardcoded `worklogs/<ID>_report.md` instead of this project's `docs/worklogs` — repaired to `docs/worklogs/${id}_report.md` (`:106`, backup `/opt/storagegenie-dispatch/dispatch_coder.sh.bak-SG004`, `bash -n` clean, mode `root:root 755` preserved). SG-004 `3294091` was already substantively valid; the repair un-breaks P5 for future slices.
- **Wrapper effort gate relaxed (owner directive, SG-004 day):** line 37 enforced medium-only (`effort_not_approved`); replaced with default-to-medium so parsed `low|medium|high|max` all pass. Owner explicitly wants non-medium efforts available (asked High). Next `high` dispatch is the functional proof.
- **Shared host (:8000 / docker):** SG-021 forensics (2026-09-08) supersede the old advisory — :8000 answers a FOREIGN Uvicorn 404 (quoted; 5173 closed), docker socket denied to the dispatch identity (exact quote). Awaiting desk Launcher#18 (access grant + occupant ID); port-remap decision returns to owner. Health via TestClient fallback stays source of truth until compose-up proves otherwise.
- **Gates (current):** `mypy 40` advisory in 9 files; backend suite 66 collected via `venv/bin/pytest`; `ruff` + `TestClient` + `npm test`/`lint`/`tsc` all binding and green (only ISS-1 decoder nodes red in-sandbox).
- **Evidence store/DB are G-K3 sensitive surfaces** (`/data/storage`, `/data/db/storagegenie.db`, host/credential) — every decision touching them needs owner sign-off individually, per `AGENTS.md`.
- **Cutover WIP absorbed (retires the D1 on-disk-WIP note):** the pre-conversion untracked code is fully superseded by the Phase 0 + Phase 1 builds above; no dispositioned obligation remains open. Kept for provenance only.
- **Effort vocabulary drift (OPEN, trivial):** `AGENTS.md` promises `xhigh`; wrapper shape is `max`. Harmless until someone dispatches either token — then it rejects. Fix with next AGENTS.md touch.
- **Queued for owner approval (not applied):** shared-proposal relay for the template-drift class (`receipt_valid` literal + finalize re-parameterization + `max`/`xhigh`); Launcher repo untouched this session.

---

## History (recent, terse)

2026-09-10: filed Launcher#25 (`provisioning-request`: opencode state-dir grant for this lane, default stays codex; owner's all-projects-all-coders principle offered for desk scope decision) — desk cadence, not waited on. Values `proposed` (desk confirms the opencode path at step 1); `DISPATCH_PATH` stays unset per original filing.

2026-09-11: Launcher#25 CLOSED (`accepted`, `closedAt 2026-09-11T11:53:55Z`, verified via `gh issue view`): `CODER_STATE_DIR` now includes `/home/andrei/.local/share/opencode`, drop-in regenerated + `daemon-reload`, Dumpster-block gap fixed on three lanes; 128 KiB single-argument limit stands for opencode packets; lane proof (actually running opencode, D154) is ours. D23/D24/D26 recorded (session-labels D1/D2/D3 relabelled — IDs taken/skipped per `G-P4`).

Self-review 2026-09-08 (Phase 1 boundary): avoidable mistake — my TIFF premise cited config, never the detector (PG-IC-09), costing SG-018's STOP + SG-019/020; counter-rule is now a standing packet line. Process improvement — environment-capability premises belong in stage plans from slice 1 (learned SG-013). Queued work all still earns its place; desk items (#18, `/opt`) pending, not mine. Proposal filed: Launcher#19 (migration-head vs head-relative rollback assertions, 3 instances).
Retired 2026-09-07 (SG-001…SG-004 era rows): recoverable from `git log --oneline`; live receipts stay in the status block above.

Full onboarding narrative: `docs/launcher-onboarding-feedback.md` (for Launcher Architect).
