# Phase 0 Tasks 11–15 review — blueprint vs implementation vs proofs (2026-09-07)

**Scope:** `inception/generic_asset_catalog_expiry_tracker_blueprint.md` v3 §14 Phase 0 exit vs `docs/superpowers/plans/2026-08-28-phase-0-foundation.md` Tasks 11–15 vs tree @ `c021826` + proofs SG-007…SG-011. Review only — no dispatch, no host touch, no product edit.
**Rule-set note:** desk `VERSION` @ `0.19.4` (read 2026-09-07); project records `0.19.1` in `AGENTS.md:4` and `0.19.3` in `STATE.md:5`. Adoption obligation is record-only per Launcher `CHANGELOG.md:14` — proposed as D1 below, not applied here.
**Autonomy frame:** L3 stage grant 2026-09-03 for Phase 0 exit, bounds/hard-stops per `STATE.md:8` (quoted in §5).

## 1. Task → proof map (first action)

| Task (plan) | Proved by | Evidence (receipt + suite + files) | Verdict |
|---|---|---|---|
| **T11** Catalog search, export manifest, job/review stubs (`plans:926-992`) | **SG-008** (report on `origin/storagegenie-evidence`, suite **23 passed**) | `exports.py` repaired: `manifest_version="1"` (`exports.py:18`), runtime `ALEMBIC_HEAD` via `ScriptDirectory` (`exports.py:21-29`, closed by SG-011), single read transaction (`exports.py:34`), `Content-Disposition: attachment` (`exports.py:76`); `test_export.py` fail-then-pass + `test_search.py` 5-fixture exact-ID sets; restore into fresh `restored.db` with count + ID-set equality; stubs assert `items==[]` envelope shape | **Proved.** Residual: restore is manifest-only (file bytes out of scope by packet design); `jobs.py:36`/`review_tasks.py:36` return `items: []` while computing unused `next_cursor` (stub-shape, not logic — intended per BM-7). |
| **T12** Frontend scaffold (`plans:996-1055`) | **SG-009** (notes receipt `d0c5a44`, rated 98; suite **3 files / 7 tests**, `tsc --noEmit` 0, `npm run lint` 0) | Tree carries `client.ts` + `client.test.ts`, `types.ts`, `App.tsx`/`main.tsx`, `vite.config.ts:6-11` (:5173 + `/v1` proxy), router + Query deps in `package.json`; SG-009 re-proved inherited 3/7 green and unmasked lint (`eslint.config.js`, `eslint 9.39.5`, mask dropped) with fire-control | **Scaffold proved to build/test/lint. Not proved:** dev-server serve, proxy round-trip, or any browser/manual check — SG-009 scope was jsdom + `tsc` + eslint only (packet §36). |
| **T13** Catalog UI (`plans:1059-1111`) | **SG-009 only** (same 3/7) | `CatalogPage.tsx` implements household selector + localStorage default (`CatalogPage.tsx:16-36`), 200 ms debounced `q` (`:19`), type/status filters (`:84-101`), cursor `Load more` (`:114-122`); `useAssets.ts:5-25` injects `household_id/q/cursor/limit=20`; `AssetCard.tsx` shows thumbnail (`:9`), name/type/status (`:54-59`), evidence count + accepted-count badge (`:61-66`) | **Implemented, behavior unproved.** Only `AssetCard` render (name/type) is test-covered (`AssetCard.test.tsx`); filter/pagination/debounce/household-default paths have zero frontend tests; no TestClient-to-UI or browser pass exists. |
| **T14** Detail + create + attach + provenance (`plans:1115-1174`) | **SG-009 only** | `AssetDetailPage.tsx` renders evidence timeline (`:97`), assertion table with field/value/source/state/confidence/evidence (`:113-141`), audit history (`:143-154`), edit via `If-Match` (`:22`), attach via upload→`POST /assets/{id}/evidence` (`:30-47`); `AssetForm.tsx` has required `display_name` (`:56-59`), type/qty/unit/condition (`:72-81`), drag-drop + SHA preview (`:38-52`), upload→create with `Idempotency-Key` (`:67-89`), success navigates to detail (`CapturePage.tsx:48`) | **Implemented, behavior unproved.** Only `ProvenanceBadge` colour is test-covered; no test exercises create→detail→attach→provenance in the UI; `EvidenceGallery`/`AssetForm` validation/upload flow unproved beyond `tsc`. Backend legs of the same flow ARE proved (SG-007 G1/G3). |
| **T15** Exit E2E + ADRs + runbook (`plans:1178-1255`) | **None yet** — SG-010 packet written, untracked (`docs/packets/SG-010-phase0-exit-e2e-adrs-runbook.md`, `git status` `??`) | `docs/adr/` absent (glob nil); repo-root `README.md` absent (glob nil — plan Task 15 step 3 + SG-010 G3 have no target); no `test_phase0_e2e.py` in `backend/tests/` (`ls-files` list ends at `test_search.py`) | **Open. SG-010 must still cover all of it** (see §2). |

Backdrop (not in the 11–15 window but load-bearing for it): **SG-007** proved Tasks 9/10 backend legs (suite **14 passed**, receipt `d43f4c1`): manual create→201 with per-field `accepted` assertions + `asset.create` audit, same-key idempotent re-POST, 403 cross-household, cursor round-trip without gaps/dups, PATCH supersession + 409 on stale `If-Match`, post-creation attach, archive→`ARCHIVED`. **SG-011** (notes receipt, rated 98, suite **23 passed**) closed the two queued findings: `evidence_service.py:95` mypy instance gone (44→43 repo-wide, no growth) and `db_revision` now runtime-derived with scratch-perturbation catch. Both keep T11's contract honest; neither advances T12–T15.

## 2. What SG-010 must still cover (no more, no less)

Packet `SG-010-phase0-exit-e2e-adrs-runbook.md:38-95` already scopes it correctly — the gap list below is a conformance check against that packet, not a re-plan:

- **G1 single-fixture E2E** (`test_phase0_e2e.py::test_phase0_exit_condition`): create→201, JPEG upload + `POST /assets/{id}/evidence`→200, `GET /assets?q=` finds it, `GET /export` contains both ids, detail shows evidence item + `display_name` assertion. Every leg asserts against ids created in legs 1–2 (packet bans fixed-id replay as vacuous). Fail-first quoted, or "already passed, hardened" with the passing run quoted.
- **G2 ADRs:** `ADR-001` (SQLite WAL + local FS now, Postgres+S3 later via same SQLAlchemy/Alembic path; migration-path test keeps it honest) and `ADR-002` (immutable evidence + assertion chain + audit on write; deferred tables `observation/identifier/classification/location/lifecycle_event/asset_relation` as recipe, never Phase 0 columns). Each traces claims to file/line — an ADR describing absent code is a finding, not an ADR.
- **G3 runbook:** `README.md` Phase 0 instructions (prereqs, `compose up`, migrate + `seed.py` Popescu + 2 users, health with expected output, both suites, data locations + gitignore reason) + **BM-8** trust note (LAN-only, single household, no auth — scoping is namespacing, never security), verified by reading every named command/path against the tree.
- **G4 UV-5 measurability:** `asset-accepted minus asset-created` from the E2E fixture's own `audit_event` rows (columns exist: `audit_event.py:15-24` action + `timestamp` server-default) — present, ordered, non-negative, no new schema. Uncomputable duration is an audit-write finding, fixed only inside the audit path on test proof.
- **G5–G7 mechanics:** full `pytest` non-vacuous + `ruff` clean quoted + `mypy` advisory count; `SG-010.log`/`SG-010_report.md` unconditional; evidence-ref prefill equality quoted before publish, `Dispatch-ID: SG-010` receipt verified as artifact. Ceiling per packet §74–85: test + ADRs + README + audit-path-only repair + worklogs + prefill/publish; no migrations, no frontend, no expiry/LLM/OCR, no prod writes.

## 3. Blueprint vs implementation (Phase 0 slice only)

- **Exit condition (§14:504):** "manually catalog an item, attach source photos, search it, export a consistent backup." Backend legs proved in isolation (SG-007 CRUD/attach/search-filters/provenance; SG-008 manifest + restore). The single-fixture walk + the durability pair (ADRs + runbook) are exactly the missing piece SG-010 owns. No blueprint change needed.
- **API (§7:334-355):** Phase 0-needed routes all present and proved: `GET/POST /assets`, `GET/PATCH/DELETE /assets/{id}`, `POST /assets/{id}/evidence`, `POST/GET /evidence` + file/thumb, `GET /export`, `GET /households` + users, stub `GET /jobs` + `GET /review-tasks`, `GET /health`. Gaps are Phase 0-consistent, not drift: no `POST /assets/{id}/events` (lifecycle covered by `status` + `ARCHIVED` soft-delete; events table deferred per plan Q1), no `/imports|/candidates` (Phase 1 job engine), no `/review-tasks/{id}/resolve` (stubs read-only per BM-7). One wart: `review_tasks.py:40-47` keeps a `/review_tasks` underscore alias alongside `/review-tasks` — harmless, worth a line in SG-010 REMAINING or a later cleanup, not an exit blocker.
- **Domain (§4:144-247):** core tables present match the Phase 0 subset (asset/evidence/assertion/audit/household/user/job/review_task + `asset_evidence` join + `idempotency_key`); deferred tables correctly absent. **BM-1 pinned:** manifest `manifest_version` (`exports.py:18`), assertion `value_json` conventions + `review_state` default `accepted` (`assertion.py:16-19`), UUIDv7 ids with `uuid4` fallback (`base.py:10-18`). **BM-2 wired + proved:** real `IdempotencyKey` table (`idempotency.py:10-18`) with in-table `response_json` assertion (`test_export.py:186-211`); known limits (second-transaction race, no TTL column) reported-not-redesigned — honest, keep. **BM-6 bound:** export reads inside one transaction (`exports.py:34`) + `db_revision` in body (`:54`); N+1 assertion loop noted in SG-008 packet but unrepaired — fine for Phase 0 scale, revisit only on evidence.
- **UX (§11:423-447):** screens present map to core 2/4/5/6 (Capture, Catalog, Detail, Settings-as-6-lite); Inbox/Review-workspace correctly absent (Phase 1). Detail meets the review-workspace behaviors that apply to manual flow (evidence visible while accepting — assertions table + gallery co-located; `unknown` valid — no forced expiry; confidence/source shown). Expiry plugin screens (§11.2) correctly absent — Phase 0 ban holds (no LLM/OCR/barcode/expiry code found in scope).
- **Security/privacy (§12:451-474):** controls observed: FS root outside web root + served via API (`local_store`, `evidence.py:94-107` file/thumb), signature/MIME validation + bomb guard + EXIF-free derivatives (SG-006, re-proved SG-011 G-behavior), provider keys never in frontend (no AI wiring), audit on writes, `.env`/db/storage gitignored. Trust boundary (BM-8) is the one unrecorded control — lands in SG-010 G3 README, not in code.
- **Testing (§13:478-491):** domain/API/media layers covered at backend (CRUD, idempotency, pagination, filters both polarities, manifest, restore, bomb, EXIF, audit); UI layer thin (3 files/7 tests — render + client URL only); evaluation corpus correctly deferred (no AI yet). The E2E + duration assertion (G1/G4) is the last §13 row for Phase 0.

## 4. Overall plan + accepted-deltas health

- Plan structure (15 tasks, TDD fail-first each) held: Tasks 1–11 backend proved through SG-005…SG-008; Tasks 12–14 implemented + scaffold-proved (SG-009); Task 15 correctly last. No task was skipped; T13/T14 test thinness is implementation debt, not plan failure — the plan ordered component tests that were only partially written (Catalog filter, form validation missing).
- Accepted deltas (`2026-09-03-accepted-optimizations.md`, owner `m0015/m0019`): landed — TS-1 busy_timeout, TS-2 bomb guard dynamic, TS-3 EXIF-free, TS-4 lockfile + pinned base, TS-5 lint unmasked (SG-009); BM-1/BM-2/BM-5/BM-6/BM-7 as above; UV-1 restore (manifest-only cut, SG-008); UV-3/UV-6 correctly rejected and absent. Outstanding by design: **UV-5** (SG-010 G4), **BM-8 note** (SG-010 G3), **BM-4** Postgres-dialect CI check (Task 1 CI item — unlanded, carry to Phase 1 or a hardening slice, not exit-blocking), **UV-2** FTS5 → Phase 1, **UV-4** push → Phase 3 boundary. Cap policy (`m0019`, no silent hard caps, 20 MB stays as visible-413 default) observed in oversize-reject test naming the configured limit.

## 5. Autonomy (L3) + standing items

- **Grant:** L3 2026-09-03, owner quote "Okay, I agree with L3" (`STATE.md:8`, `D2`). Scope: Phase 0 exit Tasks 11–15 sequenced with no per-slice approvals. SG-008 → SG-009 → SG-011 ran inside it; **SG-010 is the last L3 slice.**
- **Bounds (still binding):** codex high (`m0106` stands); temp-path fixtures only; zero prod writes/restarts/secrets/infra; one retry per slice. **Hard stops:** BLOCKED/STOP, score <95, unrecoverable dispatch refusal, any G-K1 fork or new sensitive-surface touch (surfaces: `/data/storage`, `/data/db/storagegenie.db`, host/credential per `AGENTS.md`). Nothing in this review touches a surface — review-only, no dispatch.
- **Ordering note:** SG-011 (queued-findings) ran before SG-010 (exit). Permitted under L3 sequencing (small, in-ceiling, nothing in flight at dispatch) and it de-risked SG-010 (mypy/advisory baseline + `db_revision` mechanism settled before the exit slice asserts on them). Not a precedent for reordering past a hard stop.
- **Migration/tail:** complete at `0.19.3` (SG-009 proof + SG-011 step 8, both 98 — `STATE.md:14-16`). Branch half of step 9 verified no-op (`STATE.md:15`); **only the desk's `/opt` removal is outstanding** — desk-owned, desk-observed, notified on #8; no action, no ping per U2.
- **Version drift:** Launcher `VERSION` now `0.19.4`; `CHANGELOG.md:8-14` adoption is record-only ("Record `0.19.4` as your expected rule-set version. That is the whole obligation." + three §8e provisioning deltas that bind only if provisioning). `RULES.md` byte-identical since `0.18.1` — no re-cutover, no proof re-run. Proposed as D1; per `G-L1` a mid-session delta adopts at the next between-slices boundary — SG-010's packet already embeds `0.19.3`-era header expectations, so record the stamp now, change no packet text mid-flight.

## 6. Risks / watch items for SG-010 (for the audit, not the packet)

1. `jobs.py:36` / `review_tasks.py:36` compute real queries then return `items: []` — shape-correct stub, wasted read; acceptable per BM-7, do not "fix" inside SG-010 ceiling.
2. Export N+1 assertion loop + `with db.begin()` against a TestClient-yielded session — SG-008 proved it green; if SG-010's single-fixture run trips a session-affinity edge, that is a finding with a minimal repair, not a redesign.
3. Frontend remains the thinnest proof in Phase 0 (no browser pass). Exit does not require one (plan Task 15 manual check is a runbook line, not a gate), but say so explicitly at close rather than letting "E2E" imply UI automation that never ran.
4. `SG-010` packet is untracked — commit + push before dispatch or the runner fails `packet_missing` (`DISPATCH.md:29`). No text change needed for `0.19.4` (record-only delta); the `contract_sync=refresh version=0.17.2` header in G7 is the measured wrapper echo (`D3`), not the rule-set stamp — do not hand-sync.
