SG-055 — enablement probe worklog (key + consent + restart, read-only)

- Dispatch-ID: SG-055
- Coder: opencode (per packet `coder: opencode`)
- Effort: `low` — read from process args `/proc/1384941: opencode run --auto --dir /home/andrei/StorageGenie --variant low …`
- Model: `unknown` — no model id on argv; the CLI default IS the model and is omitted per policy. Never read from an identity line.
- Contract: 0.27.0 (recorded == published payload == SG-049 receipt echo; packet states it).
- Work dir: /home/andrei/StorageGenie
- Origin: git@github.com:Andovol/StorageGenie.git
- BASE REF requested: origin/automation; resolved commit: 3b41d88e82791cfdc67fa7e90afe92c8412ad849
- Run identity: SG-055 run-2 (run-1 `1e0f5b3` STOP-as-SUCCESS, rated `79c831f`; this run measures post-M31).
- Slice wall-clock: start ~2026-09-17T11:46:00Z (probe `date` 2026-09-17T11:46:48Z); close ~2026-09-17T11:47Z.
  Budget: 120s ordinary · 300s note push+verify · 600s early-close · 900s overall.
  Actual vs budget: ~75s vs 900s overall — well under; no command was killed.
- Spend (real $): $0.000000 actual vs $0 bound. Zero provider calls.
- DATABASE: none. No DB touched, not even arithmetically. Read-only service/container probes only.

## Verdict

**GO.** All three establishments are true; the SG-049 live leg may be re-dispatched.

- (a) `GET /v1/settings/ai` → `consent` = `true`.
- (b) provider key exists → `True`, len `67` (zero key bytes).
- (c) `ai_status()` → `(True, 'enabled')`.
- (d) backend container `StartedAt=2026-09-17T11:45:59.396847235Z`, image
  `sha256:4f338b356945f9a8f960ecd3f50a3890a7c2b6d536ea4312c4f66e25e2f78f5e`, `Up 46 seconds (healthy)`.

## Raw outputs

All raw command output quoted in `docs/worklogs/SG-055_verify.log` (sections V0–V6).
Nothing was inferred from a bare exit code.

## Secrets gate

`docs/worklogs/SG-055_verify.log` §V5, quoted. Gate (a) secret-shaped patterns
(`sk-…`, `Bearer …`, `xox…`, `AIza…`, PRIVATE KEY blocks) → 0 hits. Gate (b)
completeness cross-check over every ≥40-char token: a naive `[A-Za-z0-9_+/-]{40,}`
grep matches only benign hex git object IDs / sha256 image digests / notes-ref hex,
classified out → `suspicious_non_hex_long_tokens = 0`. The `key_len=` integer line
is excluded. No key byte was printed, logged, quoted, or committed.
`docker compose config` was never run. No `.env` was read.

## Budget

Ordinary commands ~25s total against a 120s-per-command bound; note push+verify ~15s
against its 300s bound; early-close 600s not reached; overall 900s not reached.
Real metered spend: $0.000000 actual vs $0 bound (zero provider calls).

## PG-SC-09 (name-the-world)

GO here does not guarantee the live leg passes: the key could be present-but-invalid at
call time, or the env rotated between this probe and the leg. This probe reads presence +
length + consent, never validity. The live leg re-runs G0 itself and remains the authority;
this probe only prices down the "env missing / no restart" failure mode that run-1 proved real.

## UNCLEAR

- FIRST READ: whether run-2 should rewrite the worklogs (current state) or append to run-1's
  STOP content. I rewrote to the current GO state; run-1 remains in git history + delivery note.
- DURING EXECUTION: none blocking. `provider_id=fake` unchanged from SG-049 and explicitly not
  stop-grounds; proceeded on consent + key + ai_status.
- REMAINING: whether the SG-049 live leg is now re-dispatched and whether `provider_id=fake` is
  itself a remaining provision step or intended.
