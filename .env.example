# StorageGenie environment template — NAMES only; copy to `.env` and fill in.
# `.env` is gitignored; never commit key values.
OPENCODE_API_KEY=
SG_PROVIDER_ID=
SG_MODEL_ID=
SG_PER_JOB_CAP=
SG_MONTHLY_CAP=
SG_CONSENT=
